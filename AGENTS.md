# 프로젝트 작업 규칙

- API 형식이 바뀌면 `통신규격.md`를 구현과 같은 작업에서 갱신한다.
- 작업 단위가 끝날 때 `log.txt`에 날짜, 변경 이유/파일, 검증 결과, 미적용 배포 작업을 추가한다. 기존 기록은 지우지 않는다.
- 백엔드도 수정했다면 `../doran-doran/log.txt`에도 기록한다.
- 코드 주석은 판단 이유와 데이터 흐름을 이해할 수 있게 한국어로 작성한다.
- 비밀키, JWT, 개인정보 원문은 로그·문서·커밋에 넣지 않는다.
- 백엔드 main push는 Render 운영 배포를 유발하므로 두 저장소의 배포 순서를 확인한다.
