import { ParentGrowthDashboard } from "@/components/parent-growth-dashboard";

// 이전 ParentDashboard는 보존하되 이 경로에서는 새 성장 기록 화면만 활성화한다.
export default function ParentPage() {
  return <ParentGrowthDashboard />;
}
