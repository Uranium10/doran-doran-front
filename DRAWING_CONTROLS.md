# 그림 도구 조작 개선 (2026-09-24)

## 사용자 동작

- 새 모바일 종이는 900×1300 세로. 빈 예전 초안/사진에서 그림 전환/초안 삭제 후에도 같은 기준을 적용한다. 이미 그린 가로 초안은 변형을 피하려고 원래 비율을 유지한다.
- 두 손가락으로 50~600% 확대·이동. 두 번째 손가락이 닿으면 진행 중인 선을 취소하고, 모두 뗄 때까지 새 선을 시작하지 않는다. 채우기/스포이드도 손을 뗄 때 확정한다. 상단 퍼센트 버튼으로 화면에 맞춘다.
- 에어브러시는 분사기 아이콘, 스포이드는 화면에 보이는 종이색과 투명도를 합성해 전경색을 추출한 뒤 이전 붓으로 복귀한다. 스포이드가 움직이는 동안 포인터 옆 원형 표시에서 위쪽은 새로 고를 색, 아래쪽은 현재 전경색을 비교한다.
- 우측 상단의 `밑그림`과 `색칠` 레이어를 전환한다. 색칠은 밑그림 아래에 합성되어 선을 덮지 않는다. 색칠 레이어의 채우기는 두 레이어를 모두 경계로 읽으며, 지우개·선택·전체 선택은 현재 레이어 안에서만 작동한다. 예전 그림은 자동으로 밑그림으로 읽는다.
- 도구 아래 겹친 사각형은 전경색/배경색이다. 선택한 칸의 색으로 그리며, X로 두 색을 교환하면 새 전경색을 활성화한다. D로 흑백 초기화한다. 종이 바탕색은 상단의 별도 배경 버튼에서 바꾼다.
- 하단 반투명 검정 팔레트의 오른쪽 버튼에서 HSV 휠·명도/채도 사각형·정확한 색 입력·내 팔레트를 연다. 사용자 색은 이 브라우저에 최대 16개 저장하며 삭제도 가능하다.
- 원형 크기/불투명도 인디케이터를 누른 채 위로 끌면 증가하고 아래로 끌면 감소한다. 키보드 방향키·Home/End도 지원한다. 크기는 도구 안에서 클리핑하고 중앙에는 현재 확대율을 반영한 실제 표시 크기를 보여 준다. 불투명도는 체크무늬 위에 표시한다.
- 선택 영역의 8개 점은 크기 조절, 위쪽 원은 회전, 가운데는 이동이다. 회전점은 방향키로 5도씩 돌릴 수 있다. 캔버스에서 Ctrl/Cmd+A로 선 전체 선택, Ctrl/Cmd+Z와 Shift+Z로 실행 취소/다시 실행한다.
- 올가미는 선·스티커 단위 선택이다. 네 모서리를 끌면 비율을 보존하고, 네 변 중앙은 한 축만 조절한다. 채우기 영역과 픽셀 유동화 기록은 선택 대상에서 제외한다.

## 색연필과 성능

- 고정 시드의 미세 입자와 촘촘한 심지 결을 128×128 RGBA 타일로 만든다. 작은 종이 틈은 남기되 선 전체가 끊겨 보이지 않으며, 여러 번 칠할수록 색이 쌓인다. 도구 아이콘도 몸통·나무 심·끝이 구분되는 색연필 그림을 쓴다.
- 색별 타일/CanvasPattern 캐시는 최대 16개로 제한한다. 포인터 이동마다 난수를 생성하거나 전체 이미지 픽셀을 읽지 않는다.
- 프레임당 최대 한 번만 미리보기를 그린다. 새 선을 확정할 때는 기존 그림 전체가 아니라 추가 선만 그린다. 지우개 미리보기는 원본을 복원하고 한 번만 확정해 투명도가 두 번 적용되지 않게 한다.
- 이동/회전/확대는 시작 시 선 좌표에서 계산하고 손을 뗄 때 한 번 저장한다. 포인터 취소나 두 손가락 전환 시 원본으로 돌아간다. 회전은 세로/가로 종이의 픽셀 비율을 반영한다.
- 로컬 Chromium / 900×1300 종이 / 3,000점의 긴 색연필 선 / GPU readback 포함 60회: p50 **21.9ms**, p95 **25.2ms**, JPEG 내보내기 **57.4ms**. 강제 readback을 포함한 긴 선 측정이며 휴대폰 FPS 보장이 아니다.

## 검증 및 범위

- 전체 Node 회귀 테스트 70개 통과. 확대 중심 유지, 두 손가락 잠금/세 번째 터치, 값 제한, RGB↔HSV 왕복, 결정적 입자, 8개 크기 핸들, 세로 종이 회전 길이/중심, 경계 클리핑 검증 포함.
- TypeScript `--noEmit`, webpack 프로덕션 빌드 통과.
- 별도 로컬 QA 앱에서 390px 모바일 종이·HSV UI·크기/불투명도 실제 드래그·색연필 그리기·스포이드 복귀, 데스크톱 모서리 확대·회전·이동·되돌리기를 확인했다.
- 실제 Android/iOS의 멀티터치, 펜·손바닥 혼합 입력은 기기 확인이 남아 있다. 브라우저 제어 도구가 두 터치를 동시에 입력하지 못해 해당 로직은 자동 테스트로 검증했다.
- 그림 동화에 들어가면 그림을 그리기 전에 `실력 쑥쑥`과 `편하게 읽기`를 고른다. 선택값은 기존 `reading_mode` 필드로 생성 요청에 전달되며 API·DB 형식은 바뀌지 않는다.
- 과금되는 동화 생성 테스트 없음. QA 페이지·예시 그림·로컬 API 목업은 운영 저장소에 포함하지 않는다.

## 파일

- `components/workpad/sketchbook.tsx`, `sketchbook.module.css`: 화면, 그리기/터치/선택 입력
- `components/workpad/sketchbook-controls.tsx`: 재사용 원형 조절기 및 HSV 팔레트
- `lib/sketchbook-controls.ts`: 확대/이동 상태기계, HSV 및 종이결 계산
- `lib/sketchbook.ts`: 캔버스 렌더링·선택 변형
- `components/workpad/drawing-story-setup.tsx`: 세로 초안 초기화
- `tests/sketchbook*.test.cjs`: 변환·입력·질감 회귀 테스트

수정 전 파일은 고도화 폴더 `drawing-controls-work/backup`, 레이어 추가 전 파일은 `drawing-layers-work/backup`에 보관했다.

## 추가 도구와 사용감 (2026-09-24)

- 올가미를 맨 위로, 손가락을 스포이드 아래로 배치했다. 도구 선택 시 중하단에 이름이 약 1.6초 표시된다.
- 레이어는 모두 지우기 아래에 세로로 배치한다. 누르면 밑그림·색칠 이름이 펼쳐지고 1.7초 후 접힌다. 절대 위치로 배치해 종이나 도구 크기가 흔들리지 않는다.
- 상단 스티커 메뉴에 리본·해·달·별·하트·구름·꽃·나뭇잎을 제공한다. 공통 벡터 경로를 미리보기와 Canvas 출력에 사용하므로 추가 이미지 요청·디코딩이 없다. 붙인 스티커는 바로 선택되어 이동·비율 확대·회전·삭제할 수 있다. 초안과 JPEG에도 포함된다.
- 배경 메뉴에서 종이색 8개 및 직접 색 선택을 제공한다.

### 손가락: 픽셀 유동화

- 선택한 레이어를 실제 픽셀 단위로 민다. 중심에서 반경 끝까지 `(1 - 거리²/반경²)³`으로 강도를 낮춰 가장자리에서 변형과 기울기가 부드럽게 사라진다. 빠른 이동은 반경의 1/3 이하로 분할한다.
- 역방향 bilinear 보간에 premultiplied alpha를 사용해 투명 가장자리에 검은 테두리가 생기는 것을 방지한다. 밀기 범위 원형 커서와 크기·강도 조절을 제공한다.
- 브러시 주변의 작은 사각형만 getImageData/putImageData로 처리한다. 포인터마다 React 상태나 전체 픽셀 배열을 저장하지 않고 RAF에서 미처리 경로만 반영한다.
- 완료 화면을 레이어 캐시에 인계하여 손을 뗄 때 전체 변형 경로를 재계산하지 않는다. 초안에는 변형 경로만 저장하므로 base64 그림 복사본이 이력마다 쌓이지 않는다. 실행 취소/재실행 또는 초안 복원 시 같은 경로를 재현한다.
- 기록이 바뀌지 않은 레이어는 다시 그리지 않는다. 레이어별 캐시는 Canvas 수명에 연결되며 폐기 가능한 WeakMap을 사용한다. 한 번의 밀기 경로는 최대 2,048점으로 제한한다.
- 오래 누적된 변형 기록을 초안에서 처음 복원하거나 오래된 시점으로 실행 취소할 때는 재생 비용이 발생한다. 전체 편집 화면이 아니라 국소 변형에 최적화되어 있으며, Procreate와 같은 성능을 보장한다는 의미는 아니다.

### 이번 검증

- Node 전체 75개 테스트 및 TypeScript 통과. 투명 경계 RGB, 반경 밖 픽셀 보존, 비율 조절, 기존 기능 회귀 포함.
- 실제 Chromium UI에서 배경색으로 그리기, 교환 후 전경색, 레이어 이름 자동 접힘, 스티커·배경 선택, 유동화·실행 취소·재실행 동일 픽셀 확인. 390px/1280px 화면과 두 손가락 제스처 시 유동화 취소도 검증했다.
- 900×1300 Canvas / 반경48 / 구간 이동 약14px / 120회 국소 밀기: 중앙값 약0.7ms, p95 약1.6ms. 변형120점 전체 재생 약79ms, 동일 레이어 캐시 재사용 약0.1ms. 로컬 Chromium 결과이며 실제 모바일 FPS를 뜻하지 않는다.
- 관련 파일: `lib/sketchbook-warp.ts`, `lib/sketchbook-stickers.ts`, `lib/sketchbook.ts`, `lib/drawing-story.ts`, `components/workpad/sketchbook.tsx` 및 CSS, `tests/sketchbook-warp.test.cjs`. 공개 API/DB 변경 없음.
- 변경 전 파일: 고도화 폴더 `drawing-tools-v3/backup`. QA 페이지와 측정 스크립트는 운영 배포 파일에서 제외한다.
