## 2026-09-17 표지 색상 저장

- 신규 동화 생성 응답 및 비동기 작업 `result`에 `cover_color: "#B97A55"` 추가. 불투명 6자리 HEX(`#RRGGBB`)이며 대문자로 정규화한다.
- `saved_stories.content.cover_color`에 생성 당시 색상을 저장한다. 동기 API 저장과 비동기 `finish_story_generation` RPC 모두 동일하다.
- 저장 동화 단건 응답은 최상위 `cover_color`, 목록 응답은 `stories[].content.cover_color`로 전달한다. 기성 동화 JSON에도 같은 선택 필드를 사용할 수 있다.
- 프론트 타입은 구버전 서버/기존 데이터 호환을 위해 선택 필드(`string | null`)로 둔다. 누락/null/공백/잘못된 값은 기존 갈색 표지로 표시한다. 기존 갈색이면 기존 그라데이션까지 그대로 유지한다.
- 새 책은 서버의 무작위 동화 UUID를 SHA-256으로 변환해 차분한 8색 팔레트 중 하나를 고른다. 같은 동화 ID는 재시도해도 같은 색이며 HEX 값을 저장한다. 사용자 색상 선택 UI/이미지 색상 추출/추가 모델 호출은 없다.
- 앞표지·뒷표지·회전하는 표지 모두 저장색을 사용한다. 종이 본문색과 이미지 자체는 이 값의 대상이 아니다.
- DB 새 컬럼/기존 데이터 일괄 갱신은 불필요. **비동기 저장 함수 마이그레이션 `20260917_story_cover_color.sql`은 백엔드 배포 전에 적용한다.** 이전 서버 결과에 색상이 없어도 함수가 기본색을 보완한다.

## 2026-09-17: 표지·장면 시작 삽화·사이드 리모컨 / 퀴즈 근거 본문

- API 스키마 변경 없음. cover_image는 앞표지, pages[].image는 각 장면의 첫 삽화에만 표시한다. 이어지는 화면 쪽은 본문만 표시한다.
- 표지→본문→뒷표지 완료 화면 순서. 퀴즈 있는 신규 동화는 문제 풀러 가기, 없는 동화/보관함 재열람은 돌아가기다.
- 생성 동화·퀴즈 결과의 이전으로는 대시보드, 보관함 읽기의 이전으로는 라이브러리 목록으로 복귀한다. 공개 전래동화는 /#library로 돌아간다.
- 신규 퀴즈는 passage에 풀이에 필요한 원문 발췌를 포함하도록 프롬프트를 보완했다. 여러 발췌 구간의 줄바꿈을 프론트에서 보존한다. 기존 passage:null 응답과 저장 데이터의 호환성은 유지한다.
- 아래 이전 뷰어 기록의 '같은 장면마다 이미지 반복' 대신 이 절의 장면 시작 삽화 규칙을 따른다.

## 2026-09-17: 책 뷰어·장면 이미지 표시

- 서버 요청/응답은 변경하지 않는다. pages[]는 원본 장면이며 화면의 펼침 수와 다를 수 있다.
- 각 장면의 pages[].image는 그대로 표시한다. 긴 장면을 분할한 펼침끼리는 같은 이미지 URL을 사용한다.
- 컷별 이미지는 Storage의 covers/{story_id}/page-N.{jpg|png|webp}에 저장된다. 전용 pages 버킷은 만들지 않는다.
- 데스크톱 좌측 삽화/우측 본문, 모바일 상하 배치. 화면·폰트 크기별 실측 페이징, 읽던 위치 유지, 클릭/드래그/키보드 넘김을 지원한다.
- 상세 구현과 검증은 docs/BOOK_READER.md 참고.

## 2026-09-17: 풀이 이력 테이블 보호

- 운영 `quiz_results`에 RLS 및 테이블 권한 적용 완료. 익명 접근은 차단하며 로그인 사용자는 본인 풀이만 저장·조회할 수 있다. 사용자 UPDATE/DELETE는 제공하지 않는다.
- 기존 `GET /api/v1/quiz-results`, `POST /api/v1/assessments/submit` 요청/응답 형식은 유지한다. 새 DB 내부 필드 `owner_user_id`는 백엔드에서 응답에서 제외한다.
- 프론트가 owner_user_id를 보내거나 새 환경변수를 설정할 필요는 없다. 기존 Supabase JWT를 계속 사용한다.
- DB 보호는 이미 운영 적용되었다. 내부 필드 응답 제외는 이번 백엔드 코드 배포 시 반영된다.
- 기존 users/profiles/saved_stories의 RLS 보류 및 레벨 갱신/풀이 저장의 별도 트랜잭션 문제는 남아 있다.

## 2026-09-16: 팀원 추가 퀴즈 풀이 이력 API (9bd6542)

- 신규 `GET /api/v1/quiz-results?profile_id=...&story_id=...`: JWT 필요. profile_id 필수, story_id 선택. 본인 소유 프로필 검사 후 최신 생성 시각순 반환. 현재 페이지네이션 없음.
- 응답: `{ "quiz_results": [...] }`. 각 항목은 id, profile_id, story_id(null 가능), kind(initial/retest/post-story), assessment_type, total_questions, correct_answers(소수 가능), score_rate(정답 수/총 문항 수), details, level_before(null 가능), level_after, gauge_after, created_at.
- details는 question_id/level/selected_value/is_correct와 원문을 찾을 수 있는 경우 prompt/passage/options/answer/skill 스냅샷을 포함한다. is_correct는 현재 클라이언트 입력을 복사하므로 서버 계산 총점과 별도로 해석해야 한다.
- 기존 `POST /api/v1/assessments/submit`의 요청/성공 응답 형태는 유지하지만, 레벨 갱신 후 quiz_results 저장이 추가되었다. 두 저장은 원자적이지 않아 결과 저장 실패 시 레벨만 갱신될 수 있다.
- 동기/비동기 신규 생성은 saved_stories.content.quizzes를 보관한다. 보관함 재열람 API의 quizzes 빈 배열 동작은 유지한다. 프론트의 이력 화면은 아직 추가하지 않았다.
- 읽기 전용 확인 결과 운영 quiz_results 테이블 및 비동기 quizzes 저장 함수는 적용되어 있다. 저장소 SQL의 '운영 미적용' 주석은 이 확인 결과보다 이전 기록이다.
- 보안 이력: pull/검토 당시 신규 테이블은 RLS 비활성이었다. 2026-09-17 아래가 아닌 문서 상단의 보안 적용으로 해결했다.

## 2026-09-16: 동시 생성 최대5편

- 요청/응답 규격과 프론트 코드는 동일하다. 백엔드 내장 실행기가 프로세스당 최대5편을 병렬 처리한다. 6번째 이후는 queued에서 자리를 기다리며 기존 대기 문구를 사용한다.
- 같은 계정의 활성 작업1개 제한과 JWT 소유권 검사는 유지한다. 별도 계정만 동시에 생성할 수 있다.
- `GENERATION_MAX_CONCURRENT` 기본5, 범위1~5. 서버 전체를5편으로 유지하려면 웹 프로세스/인스턴스1개로 배포한다. 이미지 API 요청은 프로세스 공용2개 제한을 유지한다.

## 2026-09-16: 생성 작업을 백엔드 내부에서 처리

- API 경로, 요청/응답,202 접수, JWT 소유권 검사, 단계별 polling 규격은 변경하지 않는다.
- FastAPI 시작 시 생성 전용 스레드가 Supabase 작업 큐를 소비한다. 별도 Background Worker 서비스가 필요 없다.
- 기존 웹 서버에서 `GENERATION_JOBS_ENABLED=true`로 활성화한다. `GENERATION_EMBEDDED_WORKER` 기본값은 true이며 프론트 환경변수 추가는 없다.
- 화면 이동/새로고침은 작업을 취소하지 않는다. 체크포인트 적용 이후 신규 작업은 서버 중단 시 마지막 하트비트 후 약120초에 실행 권한이 만료되며, 같은 생성 버전의 worker가 완료 단계를 재사용해 최대2회 재개한다. 그동안 status=running과 마지막 stage를 유지한다.
- queued는 기존 방식으로 시작한다. 체크포인트가 있는 running 작업은 본문 후보/선택/퀴즈/컷별 이미지/장별 음성·시간표를 복원한다. 구버전 작업·서로 다른 생성 코드/지침/모델 설정의 작업을 임의로 혼합 재개하지 않는다.

## 2026-09-16: 전역 생성 상태 및 최신 작업 복구

- 신규 `GET /api/v1/stories/generation-jobs/current` (JWT 필요): `{available:boolean, job:GenerationJobResponse|null}`. 큐 비활성은 available=false/job=null, 활성 작업이 없으면 available=true/job=null. 최신 작업은 완료/실패도 포함한다.
- 작업 응답 공통 추가 필드: `profile_id`, `story_id`, `created_at`, `finished_at` (string 또는 null). status/stage/result/error_code/poll_after_ms는 유지한다. 다른 계정의 작업과 입력/claim_token은 반환하지 않는다.
- `POST /stories/generation-jobs`는 접수 시202와 작업 ID만 중심으로 반환한다. queued는 대기, running은 worker 선점 후 실행 중이다. 접수 성공을 생성 완료로 표시하지 않는다.
- stage는 완료한 노드 이름이 아니라 현재 실행 단계다. `retrieve → write_candidate → select → assets → (illustrations 또는 quizzes) → assemble → saving → completed`. 퀴즈/이미지 완료 순서에 따라 중간 단계가 달라진다. queued/starting/failed도 유지한다.
- 전역 GenerationProvider가 라우트 이동 후에도 조회한다. localStorage에는 계정별 작업 ID/알림·닫기 확인 ID만 저장한다. 최신 작업 API로 새로고침/다른 기기 복구. 기존 pendingGeneration/waitForGeneration은 이전 호출부 호환용이며 새 대시보드는 사용하지 않는다.
- 활성 조회1~5초(서버 현재2초), 네트워크 오류 후5초, 유휴 최신 작업 조회15초. 접수 응답 유실 시 GET으로 복구하며 POST/유료 생성 자동 재시도 없음.
- 완료 시 전역 토스트와 생성 카드에 '따끈한 동화가 완성되었어요!' 표시. 버튼 클릭 전 현재 화면을 강제 전환하지 않는다. 브라우저를 닫은 동안 OS 푸시 알림은 제공하지 않는다.
- DB 테이블/함수 적용 완료. 별도 Render worker 없이 백엔드 내부 생성 스레드를 사용한다. 기존 웹 서버의 GENERATION_JOBS_ENABLED=true 활성화가 필요하다. 그 전에는 동기 요청을 전역에서 기다리는 호환 모드이며 단계별 DB 조회/새로고침 복구는 없다.
- 구현·설정 명세: 백엔드 `backend_test/ASYNC_GENERATION.md`.

## 2026-09-16 채택 구성: Flash low 두 편 + low 선택

- 요청 필드와 기존 응답 스키마는 유지한다. 현재 기본 집필/선택 모델은 Gemini 3.8 Flash low다.
- 신규 응답의 `generation.selection.repair`는 null이다. 선택 후 내용 보충/재생성은 하지 않는다. 과거 저장 결과의 repair 객체는 호환 목적으로 유지한다.
- `selected_with_concerns`는 기준 미달 원문을 선택했다는 뜻이다. `review_failed`는 검증 실패 후 유효 원문을 반환했다는 뜻이며 통과로 해석하지 않는다.
- 신규 작업은 `select` 다음 `assets`로 이동한다. `repair_selected`는 과거 작업 호환 값으로만 남긴다.
- 아래 과거 기록의 Sol 기본값/부분 수정 단계 설명보다 이 절이 우선한다.

# API 통신규격

- 마지막 갱신: 2026-09-17
- 로컬 구현 기준이다. 운영 반영 여부는 log.txt와 배포 기록을 확인한다.
- 개인 API에는 `Authorization: Bearer <Supabase access token>`이 필수다.
- 공개 경로: `GET /`, `/api/v1/library/folktales`, `/api/v1/stories/default/{story_id}`.
- 공통 오류: 401 인증 실패, 403 다른 user_id 지정, 404 대상 없음/타인 소유, 409 사전 조건 미충족, 503 인증/DB 서비스 장애.
- 사용자 신원은 서버가 검증한 JWT로 결정한다. 기존 user_id는 호환성 검사에만 사용한다.

## 2026-09-15 생성 기능 확장 (로컬 구현, 운영 반영 전)

### 계정별 삽화 설정

- `GET /api/v1/users/me/generation-preferences`
- 응답: `{ "page_images": false, "available": true, "jobs_enabled": true }`
- `PATCH /api/v1/users/me/generation-preferences`
- 요청: `{ "page_images": true }`, 응답은 GET과 동일하다.
- 사용자 ID는 JWT에서 정한다. 프로필별 설정이 아니라 계정 전체 설정이다.
- 새 SQL/워커 준비 전에는 GET이 `available:false, jobs_enabled:false, page_images:false`를 반환한다. PATCH는 503이다.

### 비동기 동화 생성 (새 프론트 권장 경로)

`POST /api/v1/stories/generation-jobs`

헤더에는 Bearer 토큰과 `Idempotency-Key: <새 UUID>`를 보낸다. UUID는 `crypto.randomUUID()`로 만든다.
같은 계정에서 같은 키·동일 요청을 재전송하면 기존 작업을 반환한다. 다른 요청에 같은 키를 재사용하면 409다.
접수되는 `job_id`는 이 UUID와 같아 접수 응답을 잃어도 GET으로 조회할 수 있다.

```json
{
  "profile_id": "prof_abc123",
  "assessment_type": "posttest",
  "protagonist_name": "도란이",
  "favorite": "공룡",
  "today_event": "친구와 장난감을 나누기 어려웠어요.",
  "page_images": true,
  "tts": false
}
```

- `protagonist_name`: 공백 제거 후 1~30자. 프로필 이름과 다르게 입력할 수 있다.
- `favorite`: 최대 200자, `today_event`: 최대 1,000자. 둘 중 하나 이상은 필요하며 빈 항목을 다른 항목으로 복사하지 않는다.
- `page_images`: `true/false/null`, 생략 또는 null이면 계정 저장값을 사용한다. 명시한 값은 이번 요청에만 적용한다.
- `tts`: 기본 false. true이면 비동기 생성 작업에서 선택된 본문을 장별 음성으로 만든다. 동기 `/stories/generate`에서는 409를 반환한다.
- 모델은 서버 환경변수로 선택한다. 요청에서 모델명·레벨·사용자 ID를 지정할 수 없다.
- 사용자 비교 결과에 따라 기본 본문 모델은 `gemini-3.8-flash`로 유지한다. 단계별 어휘·문장 지침은 적용하되 총 분량 차이만으로 수정 호출/생성 실패를 유발하지 않는다.

202 응답:

```json
{
  "job_id": "aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa",
  "status": "queued",
  "stage": "queued",
  "result": null,
  "error_code": null,
  "poll_after_ms": 2000
}
```

`GET /api/v1/stories/generation-jobs/{job_id}`로 약 2초마다 조회한다. 응답 구조는 접수 응답과 같다.

| 필드 | 값과 의미 |
|---|---|
| status | `queued`, `running`, `completed`, `failed` |
| stage | `queued`, `starting`, `retrieve`, `write_candidate`, `select`, `repair_selected`, `assets` (구버전 `write`, `validate`도 호환), `quizzes`, `illustrations`, `assemble`, `completed`, `failed` |
| result | completed에서만 기존 동화 응답 + 아래 확장 필드 |
| error_code | null 또는 `generation_failed`, `expired`, `profile_unavailable` |

퀴즈/그림은 병렬 작업이므로 stage를 진행률 숫자로 해석하면 안 된다.
계정당 진행 중인 작업은 1개다. 추가 요청 409, 전체 대기열 50개 초과 429, 서비스 준비 전/DB 장애 503이다.
타인 작업·없는 작업은 404다. 측정 전 프로필은 409다. 화면 이탈은 조회만 취소하며 서버 작업은 계속된다.
대기30분 또는 최초 실행20분을 넘거나 복구 횟수를 소진한 작업은 큐 처리 시 expired로 실패한다. 체크포인트 재개는 같은 job_id/story_id를 사용하며 새 생성 요청을 만들지 않는다. 일반 모델 실패·안전 차단은 자동 재개 대상이 아니다.
완료/실패 이후 새로 만들기는 새 UUID를 사용한다. 완료 시 보관함 저장과 상태 변경은 하나의 DB 트랜잭션이다.

### 생성 결과 확장

기존 `assessment_type, story_id, title, theme, pages, quizzes` 구조를 유지한다.
새 맞춤 동화의 `pages`는 삽화와 연결되는 **5개 장면**이다. `pages[n].image`에는 해당 컷 이미지 URL 또는 빈 문자열이 들어간다.
프론트는 긴 장면을 화면 크기·글꼴에 따라 여러 읽기 쪽으로 나눈다. 따라서 화면의 전체 쪽 수는 API pages 길이와 다를 수 있다.
장면을 나누어도 원문/서버 page_number/저장 데이터는 변경하지 않는다. 이어지는 쪽은 같은 장면의 이미지 URL을 사용하므로 추가 이미지 생성 요청은 없다.

```json
{
  "cover_image": "https://PROJECT.supabase.co/storage/v1/object/public/covers/story_ID/cover.png",
  "pages": [{
    "page_number": 1,
    "heading": "첫 만남",
    "text": "도란이가 공룡 친구를 만났어요.",
    "image": "https://PROJECT.supabase.co/storage/v1/object/public/covers/story_ID/page-1.png",
    "image_status": "ready",
    "audio_url": null,
    "audio_status": "not_requested"
  }],
  "generation": {
    "prompt_version": "personalized-story-v5.1-stable-vocab-samples",
    "model": "gemini-3.8-flash",
    "source_id": "원전 UUID",
    "retrieval_mode": "catalog_lexical",
    "page_images": true,
    "quiz_status": "ready",
    "cover_status": "ready",
    "tts_status": "not_requested"
  }
}
```

- 위 pages는 필드 설명용 1페이지 예시이며 실제 생성 결과는 5개다.
- 장면 text는 기존 750자 제한 없이 반환한다. 공급자 출력 토큰 한도는 유지한다. 원문을 자동으로 잘라 버리지 않는다.
- 프론트 화면에서는 문장 경계를 우선하고, 긴 문장은 어절/유니코드 글자 단위로 나눈다. 개행은 표시하며 마지막 읽기 쪽에서 ‘다 읽었어요’를 보여준다.
- `image_status`: `ready`, `failed`, `not_requested`. 시트 생성 실패 시 컷별 호출을 중단하고 failed로 반환한다. 일부 컷 실패 시 다른 컷과 본문은 보존한다.
- `audio_status`: `not_requested`, `ready`, `failed` (과거 `not_implemented` 호환). `tts_status`는 전부 성공 `ready`, 일부 실패 `partial`, 전부 실패 `failed`. 저장 응답의 `audio_url`은 null이며 아래 인증 API로 만료 시간 있는 재생 URL을 받는다.
- `quiz_status`: `ready`, `failed`, `not_required`. 레벨 1~9는 퀴즈 없음, 10~20은 정상 시 7문항이다.
- `cover_status`: `ready/failed`, `cover_image`는 URL 또는 null. 컷별 삽화 미선택 시에도 기존 표지 생성은 유지한다.
- `retrieval_mode`: `catalog_lexical`, `qdrant`, `qdrant_unavailable_catalog_fallback`, `qdrant_empty_catalog_fallback`; 선택적 재정렬 시 `+rerank`, 실패 시 `+rerank_failed`가 붙을 수 있다.
- 이미지 URL은 서버가 생성·업로드한 결과만 사용한다. LLM 출력 URL은 사용하지 않는다.
- 보관함 저장에는 pages/cover_image/generation이 들어가며 다시 읽기 API에서도 반환한다. 예전 저장 동화에는 확장 필드가 없을 수 있다.
- 같은 입력으로 완성본 후보 2편을 병렬 생성한다. 각 후보의 구조/이름 검사 실패에만 최대 1회 수정한다. 분량은 수정 조건이 아니다.
- 유효 후보가 둘이면 짧은 검증 호출 1회로 본문 흐름과 단계 적합성을 비교한다. 선택된 원문 하나에만 퀴즈·이미지를 생성한다.
- 둘 다 구조/공급자 오류로 실패하면 생성 실패(동기502/비동기 generation_failed)다. 유효 후보가 하나면 추가 비교 없이 사용한다.

### 2026-09-16: 후보 비교 메타데이터

`generation.selection`이 추가된다. 기존 저장 동화에는 없거나 null일 수 있다. 생성 요청 형식은 그대로다.

```json
{
  "strategy": "parallel_two_v1",
  "status": "selected",
  "candidate_count": 2,
  "valid_candidate_count": 2,
  "selected_candidate": "B",
  "review_version": "story-choice-v3",
  "review_model": "gpt-5.6-sol",
  "coherence_score": 4,
  "level_fit_score": 3
}
```

| status | 의미 |
|---|---|
| selected | 두 유효 후보를 비교해 선택. 선택된 후보의 두 점수가 모두3 이상. 품질 보증은 아님 |
| selected_with_concerns | 비교했지만 선택된 후보의 흐름 또는 단계 점수가3 미만. 현재는 선택한 원문을 그대로 반환 |
| single_candidate | 유효 후보가 한 편이라 검증 호출 생략. review_model/점수는 null |
| review_failed | 검증 통신/스키마 오류. 유효 후보 A를 결정적으로 반환하고 점수는 null |

- 점수는 LLM의 0~4 상대 평가다. 단계 적합성2 미만의 큰 이탈을 우선 피하고, 그 안에서 흐름 → 단계 점수 → 동점 A 순으로 고른다.
- 후보 원문 두 편과 검토 근거는 내부 상태에만 있으며 API에는 선택된 완성본 하나와 위 요약만 반환한다.
- `prompt_version`은 롤백한 집필 지침 v5.1을 나타낸다. 비교 방식은 `selection.strategy`, 검증 지침은 `selection.review_version`으로 별도 식별한다.
- 새 진행 단계 `write_candidate`는 두 병렬 분기에서 각각 완료 알림을 낼 수 있다. `select` 완료 뒤 선택된 본문의 자산 생성을 시작한다. stage는 백분율이 아니다.

### 기존 동기 경로

`POST /api/v1/stories/generate`는 기존 200 동화 응답을 유지하고 내부는 같은 LangGraph를 사용한다.
입력 확장은 위 비동기 요청과 같다. `page_images:false`를 명시하면 기존 계정 설정과 무관하게 표지만 생성한다.
컷별 삽화가 켜진 요청은 409로 비동기 경로 사용을 안내한다. 생성 실패는 502다.
프론트는 설정 응답의 jobs_enabled가 true면 비동기 경로, false면 동기 경로를 사용한다.

---

## 구글 계정 로그인 (front -> back)
### DB에 해당 유저가 없으면 새로 생성하고 있으면 통과한다.

- 엔드포인트: POST /api/v1/users/sync
- Request: 본문 없음. 사용자 ID·이메일·이름은 서버가 검증한 Auth 사용자 정보에서 가져온다.

- Response: { "status": "ok", "message": "User synced" }

## 프로필 목록 불러오기 (back -> front)

- 엔드포인트: GET /api/v1/profiles
- Response :
{
  "profiles": [
    {
      "id": "prof_abc123",
      "name": "도란이",
      "birth_date": "2020-05-10",
      "avatar_url": "/avatars/child-1.png",
      "level": null // 아직 측정 전이면 null
    }
  ]
}

## 새 프로필 생성 (front -> back)

- 엔드포인트: POST /api/v1/profiles
- Request :
{
  "name": "우주",
  "birth_date": "2021-08-15",
  "avatar_url": "/avatars/child-2.png"
}

- Response: 새로 생성된 프로필 객체 반환 (위 profiles 배열 안의 단일 객체 형태)
{
  "id": "prof_abc123",
  "name": "도란이",
  "birth_date": "2020-05-10",
  "avatar_url": "/avatars/child-1.png",
  "level": null // 아직 측정 전이면 null
}

## 프로필 수정 

### 수정내용 반영 (front -> back)

- 엔드포인트: PATCH /api/v1/profiles/{profile_id}
- request: 
{
  "name": "도란이(수정)",
  "birth_date": "2020-06-10",
  "avatar_url": "/avatars/child-3.png"
}

### 수정된 프로필로 다시 불러오기 (back -> front)
- response: 
{
  "id": "prof_abc123",
  "name": "도란이(수정)",
  "birth_date": "2020-06-10",
  "avatar_url": "/avatars/child-3.png",
  "level": 14 
}

===========================================================================

## 폼 입력 -> 동화/퀴즈 생성 -> 채점 -> 결과 반영 파트 (back -> front)

### 동화(+퀴즈) 요청 (front -> back)

#### 분기 1: 폼 입력 후 맞춤형 동화 및 퀴즈 생성 요청
- 목적: 아이의 폼 입력을 바탕으로 새로운 동화와 퀴즈를 생성
- 엔드포인트: POST /api/v1/stories/generate
- Request:
{
  "profile_id": "prof_abc123",
  "assessment_type": "posttest", 
  "protagonist_name": "도란이",    
  "favorite": "공룡",            
  "today_event": "오늘 유치원에서 놀았어요.",
  "page_images": false,
  "tts": false
}

#### 분기 2: 배치고사 문제(문해력 측정 테스트) 생성 요청 (동화 제외)
- 목적: 동화 없이 아이의 수준을 파악하기 위한 배치고사 문제 출제
- 엔드포인트: POST /api/v1/assessments/generate
- Request:
{
  "profile_id": "prof_abc123",
  "assessment_type": "pretest" 
}

#### 분기 3: 보관함(라이브러리)에 저장된 특정 동화 요청 (퀴즈 제외)
- 목적: 이전에 생성하여 DB에 저장해둔 동화를 다시 읽기 위함
- 엔드포인트: GET /api/v1/stories/saved/{story_id}?profile_id={profile_id}
- Request: (URL Path와 Query Parameter로 전달하므로 Request Body 없음)

#### 분기 4: 자체 제공(기본) 전래동화 요청 (퀴즈 제외, 비로그인 가능)
- 목적: 로그인 없이 또는 기본 라이브러리에서 제공하는 기성 동화 열람
- 엔드포인트: GET /api/v1/stories/default/{story_id}?profile_id=guest
- Request: (URL Path와 Query Parameter로 전달하므로 Request Body 없음)


### 서버에서 동화 + 퀴즈 전송(공통 규격) - (back -> front)

- 배치고사 시에는 퀴즈만 묶어서. 동화 생성시엔 동화와 퀴즈를 묶어서. 라이브러리 조회시나 기본 제공 동화의 경우엔 동화만. 나머지는 null이나 빈 배열 처리

- Response: 
{
  // 평가 유형: "pretest"(아동 배치고사/재시험), "posttest"(동화 퀴즈), "checklist"(영유아 부모용)
  "assessment_type": "pretest", 
  
  // ▼ 동화 파트 (posttest일 경우에만 값이 들어가고, 배치고사나 그냥 보는 테스트면 null/빈 배열 처리)
  "story_id": "story_102938",
  "title": "공룡 티라노의 사과",
  "theme": "용기",
  "pages": [
    {
      "page_number": 1,
      "image": "/popup/page-village.png",
      "heading": "1. 공룡 마을의 아침",
      "text": "옛날 옛적, 용감한 공룡 티라노가 살았어요."
    }
  ],
  
  // ▼ 퀴즈 파트 (배치고사, ) - 프론트엔드의 AssessmentQuestion 타입을 정확히 따름
  "quizzes": [
    {
      "question_id": "q_045",
      "skill": "이해",                   // 문제 유형 (어휘, 이해, 추론 등)
      "passage": "토끼가 깡충깡충 뛰어 풀밭으로 갔어요.", // 예문 (체크리스트 등 예문이 없으면 null)
      "prompt": "토끼가 어디로 갔는지 고르세요.",         // 실제 질문 내용
      "options": [
        { 
          "label": "강가", 
          "value": "a", 
          "image_url": null             // 이미지가 없으면 null
        },
        { 
          "label": "풀밭", 
          "value": "b", 
          "image_url": "https://.../grass.png" // 그림 퀴즈일 경우 썸네일 URL
        }
      ],
      "level": 12,                      // 난이도 가중치 (추후 서버 채점 시 프론트에 안 줘도 됨)
      "answer": "b"                     // 정답 (추후 서버 채점 시 프론트에 안 줘도 됨)
    }
  ]
}

### 문제나 체크리스트 완료 후, 프론트에서 서버로 보낼 결과 내용 

- 엔드포인트 : POST /api/v1/assessments/submit
- Request: 
{
  "profile_id": "prof_123abc",          // 퀴즈를 푼 아이의 프로필 ID
  "assessment_type": "pretest",         // 출제 시 받았던 평가 유형 그대로 반환
  "story_id": null,                     // 동화 퀴즈였다면 해당 story_id 반환
  
  "results": {
    "total_questions": 5,               // 전체 문제 수
    "correct_answers": 4,               // 맞춘 문제 수 (프론트 1차 채점 결과)
    "details": [
      { 
        "question_id": "q_045", 
        "level": 12,                    // 해당 문제의 난이도
        "selected_value": "b",          // 사용자가 최종 선택한 보기의 value (서버 교차 검증용)
        "is_correct": true              // 프론트엔드 자체 채점 정오답 여부
      },
      { 
        "question_id": "q_046", 
        "level": 14, 
        "selected_value": "a", 
        "is_correct": false 
      }
    ]
  }
}

### 서버에서 채점결과 반영 후 클라이언트로 보낼 진척도 정보

- Response: 
{
  // 측정 종류: "initial"(최초 측정), "post-story"(동화 읽은 뒤), "retest"(재시험)
  "kind": "initial",
  
  // 성적 데이터
  "correct": 4,                         // 맞춘 문항 수
  "total": 5,                           // 전체 문항 수
  "delta": null,   // 직전 측정 대비 레벨 변화량. 최초 측정이면 null (예: +0.35)
  
  // 환산된 최종 레벨 데이터 (프론트 진행 막대 렌더링용)
  "level": 14,                          // 백엔드가 추세/가중치를 반영해 계산한 최종 20단계 중 레벨
  "lowerLabel": "나무 2단계",              // 막대 왼쪽 라벨 (현재 확실히 도달한 단계)
  "upperLabel": "나무 3단계",              // 막대 오른쪽 라벨 (다음 목표 단계)
  "achievement": 65.5                   // 두 단계 사이에서의 상세 달성도 (0~100 사이, 소수점 1자리)
}

===========================================================================

# 라이브러리에 동화 추가

## 보관함: 저장된 동화 목록 불러오기 (back -> front)

- 엔드포인트: GET /api/v1/stories?profile_id={profile_id}
- Response :
{
  "stories": [
    {
      "story_id": "story_20260622_081530",
      "profile_id": "prof_123abc",
      "title": "공룡 티라노의 사과",
      "theme": "용기",
      "created_at": "2026-06-22T08:15:30Z",
      // content 안에는 동화 생성 시의 pages 배열이 통째로 들어있음 (퀴즈 제외)
      "content": {
        "cover_image": image_url....,
        "pages": [
          {
            "page_number": 1,
            "image": "/popup/page-village.png",
            "heading": "1. 공룡 마을의 아침",
            "text": "옛날 옛적, 용감한 공룡 티라노가 살았어요."
          }
        ]
      }
    },
    {
      "story_id": "story_20260621_142000",
      "title": "길동이의 모험",
      "theme": "지혜",
      "created_at": "2026-06-21T14:20:00Z",
      "content": { ... }
    }
  ]
}

## 보관함: 동화 삭제하기 (front -> back)

- 엔드포인트: DELETE /api/v1/stories/{story_id}
- Response :
{
  "status": "ok",
  "message": "Story deleted successfully"
}

### 비로그인 라이브러리 - 기성 전래동화 목록 조회
- **Endpoint**: `GET /api/v1/library/folktales`
- **Description**: 비로그인/게스트 유저를 위한 자체 제공 전래동화(기본 동화) 목록과 화면 배치용 행(Row) 데이터를 가져옵니다.

**Response (200 OK)**
```json
{
  "folktales": [
    {
      "story_id": "default_001",
      "title": "흥부와 놀부",
      "theme": "우애",
      "description": "마음씨 착한 흥부와 제비의 보은 이야기",
      "thumbnail": "/placeholder.svg"
    }
  ],
  "rows": [
    {
      "row_id": "row_1",
      "title": "🌟 도란도란 인기 전래동화",
      "items": ["default_001", "default_002"]
    }
  ]
}
```

### 2026-09-16: 두 후보 모두 부족할 때 부분 보수

`generation.selection.repair`가 추가된다. 이전 저장 결과에는 없거나 null이다. 생성 요청 형식은 그대로다.

```json
{"status":"accepted","attempts":1,"changed_pages":[4,5],"prompt_version":"story-partial-v1"}
```

- **두 유효 후보 모두** 인과/단계 중 하나 이상이3 미만이고, 선택된 후보에 실제 본문 인용이 확인되는 문제 장면이 있어야 실행한다. 한 편만 낮은 점수, 검증 API 실패, 정상 본문 없음은 실행 조건이 아니다.
- 전체 동화를 참고하되 문제 장면과 바로 인접한 장면 중 최대3개만 수정한다. 나머지 페이지와 제목/주제/인물/화풍은 서버가 원문 그대로 보존한다.
- 수정1회와 원본/수정본 재검증1회가 최대다. 두 점수가 모두 나빠지지 않고 하나 이상 증가하며 두 점수가 모두2 이상일 때만 수정본을 채택한다. 동점·미개선·오류는 원본 유지.
- 수정 후에도 한 점수가3 미만이면 상위 status는 selected_with_concerns다. 최초 후보 ID는 유지하며 changed_pages는 실제 채택한 변경 장면 번호만 담는다.
- not_needed: 조건 미충족 / skipped_no_evidence: 실제 인용 근거 없음 / skipped_scope: 문제 장면이3개 초과. 이때 attempts=0.
- accepted: 수정 채택 / retained_original: 개선 기준 미충족 / repair_failed: 수정 호출 실패 / invalid_patch: 범위·구조·본문 변경 검사 실패 / recheck_failed: 재검증 실패. 이때 attempts=1. 채택 외 changed_pages는 빈 배열.
- 이미지·퀴즈는 수정 여부가 확정된 최종 본문 한 편에만 생성한다. 진행 단계 repair_selected는 폴백을 거쳤을 때만 발생한다.
- 검증 버전은 story-choice-v3(문제 장면·원문 인용 추가), 출력 한도16384토큰(추론 포함). 부분 수정은 최대4096 출력토큰. 집필 프롬프트 v5.1은 그대로 유지한다.


### 2026-09-16 검증 모델 설정

- 요청/응답 필드와 status 종류는 그대로다. 새 생성 결과의 review_model 기본값은 gpt-5.6-sol, review_version은 story-choice-v3다. 이전 저장 결과의 모델/버전은 보존한다.
- 서버의 STORY_REVIEW_EFFORT 기본값은 Sol medium이며, 최초 선택과 수정 재검증에 동일 적용한다. 프론트가 모델/effort를 지정하는 요청 필드는 없다.
- 검증 호출당 HTTP 읽기 제한120초, 추론 포함 최대16384 출력토큰이다. 생성 진행 중 폴링을 유지하며, 검증 장애는 기존 review_failed/recheck_failed 계약대로 처리한다.
- 집필/부분 편집 모델과 API의 5장면 구조는 유지한다. quote는 한 장면에서 연속된 실제 인용 하나를 사용하며 검증/수정 근거는 공개 결과에 포함하지 않는다.


## 2026-09-17 내 책장·읽기 기록·답안 결과지

배포 순서: `backend_test/sql/20260917_bookshelf_and_results.sql` → 백엔드 → 프론트.
DB 마이그레이션은 2026-09-17 적용 및 롤백 통합검증 완료. 기존 users/profiles/saved_stories RLS는 변경하지 않았다.
아래 API는 모두 Bearer JWT 인증과 프로필/동화 소유권 검사가 필요하다.

### 책장 요약 목록

`GET /api/v1/stories/bookshelf?profile_id={id}&offset=0&limit=12`

- limit: 1~24, offset: 0 이상. 잘못된 범위는 422.
- 응답: `{stories: BookSummary[], total: number, next_offset: number|null, resume: BookSummary|null, latest_result: object|null, measured_today: boolean}`.
- BookSummary: `story_id, title, theme, created_at, cover_color, cover_image, reading_progress`.
- reading_progress: `{bookmark, completed_at, updated_at}` 또는 null.
- resume: 최근에 읽었고 아직 완독하지 않은 책의 `story_id, title, cover_color, cover_image`. 최근 목록 범위 밖의 책도 찾는다.
- latest_result: 최근 결과의 `id, kind, created_at, correct, total`.
- measured_today: 한국 날짜 기준 해당 프로필의 새 회차 방식 측정 완료 여부.
- 본문 pages, quizzes, generation 메타데이터는 목록 응답에 포함하지 않는다. SQL에서도 해당 내용 전체를 API로 가져오지 않는다.
- 동률 정렬: created_at DESC, story_id DESC. 다음 조회는 next_offset을 사용한다. 동시 추가/삭제가 많은 경우 목록을 처음부터 새로고침할 수 있다.
- 기존 `GET /stories`는 이전 프론트 호환을 위해 유지하지만 새 화면은 호출하지 않는다.

### 동화 상세 계약 변경

`GET /api/v1/stories/saved/{story_id}?profile_id={id}`

- 저장된 quizzes가 있으면 `assessment_type: "posttest"`, 해당 문항 배열을 함께 반환한다.
- 문제가 없으면 기존처럼 `assessment_type: "library", quizzes: []`.
- 기성 동화 readonly API는 변경하지 않았다.
- 이전 문서의 ‘보관함은 항상 퀴즈 없음’ 설명보다 이 절의 계약이 우선한다.

### 서버 책갈피

`GET /api/v1/stories/{story_id}/reading-progress?profile_id={id}` → `{progress: ReadingProgress|null}`

`PUT /api/v1/stories/{story_id}/reading-progress`

```json
{"profile_id":"prof_example","bookmark":{"kind":"text","sceneIndex":1,"offset":120}}
```

- bookmark.kind: cover / ending / text / image.
- text/image는 0부터 시작하는 sceneIndex와 원문 offset을 사용한다. offset은 JS 문자열의 UTF-16 위치다.
- cover/ending은 kind만 저장한다. 범위가 잘못되면 422.
- 응답은 GET과 동일한 progress 객체. completed_at은 ending에 처음 도달한 시간이며 다시 읽어도 유지한다.
- 프론트는 빠른 장 넘김을 650ms로 묶고 동일 책의 쓰기를 순서대로 수행한다. 화면 이탈/탭 숨김 시 마지막 위치를 전송한다.
- 네트워크 실패 시 계정·프로필별 sessionStorage에 미전송 위치를 남기고 재접속 또는 다시 열 때 재시도한다. 페이지 강제 종료 직전 요청까지 항상 성공한다고 보장하지 않는다.
- 서버 위치를 먼저 복구하되 이 탭에 미전송 위치가 있으면 그 위치를 우선한다.

### 시험 회차와 하루 1회 측정

`POST /api/v1/assessments/generate` 기존 본문을 유지하며 응답에 `attempt_id` UUID가 추가된다.

- 서버가 문항 스냅샷을 회차에 저장한다. 미완료 회차는 다시 발급하지 않고 재개한다.
- 배치/재측정(부모 체크리스트 포함)은 한국 날짜 기준 프로필당 완료 1회. 완료 후 새 회차 요청은 409와 사용자용 안내 문자열.
- 독후 퀴즈는 하루 제한에 포함하지 않는다.

`POST /api/v1/assessments/story/{story_id}?profile_id={id}`

- `{attempt_id, quizzes, result}` 반환. 해당 책의 이미 완료한 새 회차가 있으면 result를 돌려준다.
- 저장된 문제가 없는 책은 409. 프론트가 주장하는 정답만으로 옛 책을 채점하지 않는다.

`POST /api/v1/assessments/submit`

- 기존 본문에 `attempt_id`를 추가한다.
- results.details의 question_id와 selected_value만 채점 입력으로 사용한다. 클라이언트 correct_answers / total_questions / is_correct / level은 채점 근거로 사용하지 않는다.
- 질문 ID 중복·누락·다른 보기 값은 422. 타 계정/프로필/동화의 회차는 404.
- DB 함수가 서버에 저장된 문항으로 채점하고 결과 저장·프로필 단계 반영을 같은 트랜잭션에서 수행한다. 동일 회차 재제출은 최초 결과를 반환한다.
- 기존 화면 호환을 위해 attempt_id 누락 시 서버가 정식 문항으로 회차를 연결한다. 문항이 맞지 않으면 422이므로 화면을 다시 열어야 한다.
- 기존 점수별 단계 증감과 최초 연령 배치 공식을 유지한다. 최종 단계 상한은 20으로 제한한다.
- 기존 응답에 `result_id`, `assessment_type`, `details`가 추가된다.
- details: `{question_id, prompt, passage, options, answer, skill, selected_value, is_correct, ...}`. 체크리스트 is_correct는 null.

### 결과지 재조회

`GET /api/v1/quiz-results/{result_id}?profile_id={id}`

- 제출 응답과 같은 화면용 형태: `result_id, kind, correct, total, delta, level, lowerLabel, upperLabel, achievement, assessment_type, details`.
- 이전 기록에 상세 문항이 없으면 화면에서 상세 미저장을 안내한다. 추가 LLM 호출은 없다.

### 화면 경로와 복귀

- `/dashboard`: 내 책장. 단계 진척도, 측정 진입, 최근 책, 생성, 이어 읽기, 최근 결과.
- `/stories/new`: 동화 생성 폼. 접수 후 내 책장으로 이동하며 기존 전역 생성 작업은 계속된다.
- `/library`: 전체 책장. 12권씩 더 보기, 표지/목록 전환, 삭제.
- `/books/{story_id}?from=dashboard|library`: 동화 상세. `&view=quiz`는 같은 책의 문제 화면.
- `/results/{result_id}?from=dashboard|library`: 결과지. 새로고침/직접 진입으로도 다시 조회한다.
- from은 library만 예외로 허용하며 나머지는 dashboard로 정규화한다. 외부 URL은 복귀 주소로 사용하지 않는다.
- 로그인 뒤 ‘홈’은 내 책장으로 통일. 랜딩은 프로필 메뉴의 ‘서비스 소개’로 이동한다.

### Kiwi 중지

- 웹 lifespan / 독립 worker / LangGraph assemble의 import·초기화·분석 호출을 주석 처리했다.
- Kiwi 전용 패키지와 setuptools 핀도 주석 처리했다. 분석기 원본 코드는 팀원의 외부 분리를 위해 남겼다.
- `generation.vocab_analysis`는 null이며 외부 분석 API는 아직 연결하지 않았다. 동화 생성·이미지·퀴즈는 기존 흐름을 유지한다.

- 배포 통합: 팀원 bffcb39/d69ed24의 Kiwi 배치 분리를 보존한 백엔드 a3ff65c push 완료. vocab_examples 저장 유지, 웹/worker는 Kiwi 미로딩. 신규 API 준비 확인 후 프론트 push 순서로 반영한다. 팀원 워크플로 경로 github/workflows는 .github/workflows로 정리해야 Actions 자동 인식이 가능하며, 배치 운영 설정은 이번에 변경하지 않았다.


## 2026-09-17 동화 어휘 분석 조회

`GET /api/v1/stories/{story_id}/vocab-analysis?profile_id=...`

- 기존 Supabase Bearer JWT 필수. 검증된 사용자 소유 프로필/동화만 조회 가능.
- `200 {"status":"pending","analysis":null}`: 분석 없음 또는 구형/불완전 필드.
- `200 {"status":"ready","analysis":{...}}`: `총_내용형태소_수`(반복 포함 등장 횟수), `상위등급_어휘_목록`/`상위등급_어휘_개수`, `표현_어휘_목록`/`표현_어휘_개수`(각 중복 제거 종류 수). 0과 빈 배열은 정상 완료.
- 조회 응답에는 본문/이미지/비율/프롬프트 예시/적중률을 포함하지 않는다. 목록 길이와 개수는 일치해야 한다.
- 오류: 인증 없음/무효 401, 미소유 또는 없는 동화·프로필 404, DB·인증 서비스 연결 장애 503.
- 기존 저장 동화 조회 응답의 `generation.vocab_analysis`도 초기 표시용으로 사용할 수 있다. 프론트는 null/필드 누락을 런타임 검증한다.
- 마지막 페이지에서만 최대 12회, 10초 간격 조회. 화면 이탈 시 취소, 숨은 탭에서 중지. 만료/오류에는 다시 확인 버튼 제공.
- 저장 JSON의 `schema_version:2`는 배치 갱신용이며 사용자 화면에는 표시하지 않는다.
- 최근 main.py 갱신에서 누락된 bookshelves/reading-progress/assessment-attempt/result 상세 API를 기존 문서 규격대로 복구했다. 정답/회차/결과 저장 계약은 변경하지 않는다.


## 2026-09-18 부모님 관리 화면과 완독 재열람

모든 아래 API는 기존 Authorization: Bearer <Supabase access_token> 인증 사용. user_id는 요청하지 않으며 JWT 계정으로 판정한다.

### GET /api/v1/parent/overview

응답 `{ "children": [...] }`. 각 아이 항목: `id`, `name`, `avatar_url`, `level`(nullable), `guidance`(string), `books_total`(int), `books_read`(int), `quizzes_completed`(int), `skills:[{name,total,correct}]`, `recent_results:[{id,kind,created_at,correct_answers,total_questions,level_after,story_title}]`.

2026-09-19: `story_title`(string|null)은 해당 퀴즈에 연결된 실제 생성 동화의 제목이다. 부모님 최근 결과지는 `제목 + " 퀴즈"`로 표시한다. 삭제된 동화/연결 정보가 없는 기록은 null이며 `동화 퀴즈`로 표시한다. 읽기 단계 측정은 null이고 기존 문구를 유지한다. 구버전 응답에는 필드가 없어도 동작한다. 제목은 계정과 프로필 범위를 확인해 조회하며 본문/이미지는 추가로 전송하지 않는다.

skills는 최근 30회 중 체크리스트 제외/skill 및 boolean 정답 판정이 있는 문항만 합산. 데이터 없음은 빈 배열. recent_results는 최근 5건. 로그인한 계정의 모든 아이를 반환하며 동화 본문/삽화/답안 전체를 내려주지 않는다. 부모님은 계정에 고정된 관리 역할이며 profiles 목록에 아이 행으로 추가하지 않는다.

### PUT /api/v1/parent/profiles/{profile_id}/guidance

요청 `{ "guidance": "친구에게 인사하는 용기를 담아 주세요." }` (최대 1,000자, 빈 문자열로 해제).
응답 `{ "profile_id": "...", "guidance": "trim된 저장값" }`.
401: 로그인 필요, 404: 타인/없는 프로필, 422: 길이 등 검증 오류, 503: DB 처리 불가.
다음 생성 접수 시 서버가 값을 읽어 내부 request.parent_guidance로 스냅샷한다. 생성 API의 클라이언트 요청 스키마는 바꾸지 않는다. 기존/실행 중 동화는 변경하지 않으며 단계별 언어 규칙보다 낮은 우선순위의 참고 데이터로 사용한다.

### GET /api/v1/stories/{story_id}/reading-progress?profile_id=...

기존 progress에 `quiz_completed?: boolean` 추가. 해당 소유자/프로필/동화의 확정 quiz_results 존재 여부. progress=null은 그대로 유지한다. PUT에는 추가 필드를 요구하지 않는다.
프론트는 bookmark.kind=ending && completed_at && (quiz_completed || 퀴즈없음)일 때 cover로 연다. 미완료 퀴즈·다시 읽던 중간 위치는 복원한다.
결과지의 from=parent는 /parent로 복귀한다. library/dashboard 기존 복귀 유지.


## 2026-09-18 책장 상태 태그와 최신 미열람 동화

기존 GET /api/v1/stories/bookshelf?profile_id=...&limit=...&offset=... 응답에 아래 필드를 추가한다. JWT 및 프로필 소유권 검사는 동일하다.

- `stories[].reading_status`: `new` / `reading` / `quiz_pending` / `completed`.
  - 본인·해당 동화 채점 기록 있음 -> completed (과거 읽기 기록 없음도 포함).
  - 완독 기록 있음 + quizzes가 비어 있지 않은 배열 -> quiz_pending.
  - 완독 기록 있음 + 퀴즈 없음 -> completed.
  - 그 외 읽기 기록 있음 -> reading, 기록 없음 -> new.
- `latest_unread`: 현재 프로필 전체 책장에서 읽기/채점 기록 모두 없는 최신 책 요약 또는 null. `story_id,title,theme,created_at,cover_color,cover_image,reading_status:"new",reading_progress:null`. 페이지의 limit/offset 범위 밖에서도 찾는다.
- 기존 stories/total/next_offset/resume/latest_result/measured_today 필드는 유지한다. 전체 본문/이미지 원본은 전송하지 않는다.

대시보드 생성 카드는 queued/running 작업을 우선 표시하고, 활성 작업이 없을 때 latest_unread에 연결한다. 둘 다 없으면 렌더링하지 않는다. 완료 job의 오래된 result를 완료 카드 링크로 사용하지 않는다.


## 2026-09-18 스티커북과 동화 퀴즈 재도전

아래 API는 모두 기존 `Authorization: Bearer <Supabase access_token>` 인증과 프로필 소유권 검사를 사용한다. user_id/정답 수/지급 종류는 요청받지 않는다. 401 로그인 필요, 403 소유권 거부(일부 기존 조회는 404), 422 입력 오류, 503 DB 사용 불가 규칙을 따른다.

### GET /api/v1/stickers?profile_id=...&limit=24&offset=0&story_id=...

limit은 1~48, offset은 0 이상. story_id는 생략 가능하며 결과지에서는 해당 책만 필터링한다. 필터 후 페이지네이션한다.

```json
{
  "stickers": [{
    "id": "획득 UUID",
    "story_id": "동화 ID",
    "title": "획득 당시 동화 제목",
    "reward_kind": "mastery",
    "earned_at": "2026-09-18T01:00:00+00:00",
    "asset_status": "ready",
    "image_url": "https://.../storage/v1/object/sign/stickers/...?token=...",
    "speaker": "토끼",
    "message": "함께 노래해서 기뻤어!",
    "story_available": true
  }],
  "total": 1,
  "next_offset": null
}
```

- reward_kind: mastery(전체 정답)만 지급·반환한다. 문항 수가 1개 이상인 독후 퀴즈에서 모두 맞혔을 때, 프로필·동화당 한 장만 지급한다. 부분 정답에는 지급하지 않는다.
- asset_status: queued / running / ready / failed. 획득과 그림 준비는 독립이다.
- image_url: 비공개 이미지의 1시간 서명 URL 또는 null. 준비 중·실패·서명 장애면 null일 수 있다. 영구 주소로 저장하지 말고 API를 다시 조회한다. 내부 image_path는 노출하지 않는다.
- 준비 중에는 기본 화자/공통 인사를 보여주며 준비 후 실제 화자/인사로 바뀐다. UI는 문자를 이미지에 합성하지 않는다.
- 과거 friend 획득 기록은 DB에 보존하되 목록·total·next_offset 계산에서 제외한다. 기존 mastery의 ID·획득일·이미지는 유지한다. 기존 이미지 재생성 없음.
- story_available=false면 원본 책이 삭제되었으므로 동화 링크를 숨긴다. 획득 기록은 유지한다.
- 빈 목록은 stickers=[], total=0, next_offset=null. 자동 조회는 10초 간격 최대 12회, 숨은 탭에서는 중단한다. 새로고침은 조회이며 실패한 이미지의 유료 재생성을 예약하지 않는다.

### POST /api/v1/assessments/story/{story_id}/practice?profile_id=...

본문 없음. 해당 동화의 첫 풀이 완료 후에만 가능하다. 진행 중 회차가 있으면 재사용하며 없으면 저장된 문항으로 is_practice=true 회차를 생성한다.

```json
{"attempt_id":"UUID","quizzes":[],"result":null,"is_practice":true}
```

quizzes는 기존 AssessmentQuestion 배열이며 빈 배열은 예시상 생략 표현이다(실제 퀴즈 없는 책은 시작 불가). 이후 제출은 기존 attempt_id 기반 채점 API를 사용한다. 재도전은 결과를 저장하지만 프로필 level과 부모님 skill 통계는 올리지 않는다. 조건을 달성하면 아직 없는 스티커만 지급한다.

### GET /api/v1/assessments/attempts/{attempt_id}?profile_id=...&story_id=...

위와 같은 응답. 계정·프로필·동화·회차를 함께 검사한다. 완료된 회차는 result가 들어있다. 재도전 페이지 새로고침은 이 조회를 사용하며 POST로 새 회차를 만들지 않는다.

### 기존 응답의 추가 필드

- 동화 퀴즈 시작 응답: is_practice boolean.
- 채점 result / 개별 결과 조회: story_id(string|null), is_practice(boolean, 기존 기록 false). 프론트의 오래된 로컬 결과에는 필드가 없을 수 있다.
- 동화 생성 및 저장 content.generation: characters(list, 기본 []), visual_style(string|null), protagonist_name(string|null), character_sheet_path(string|null). character_sheet_path는 서버 재사용용 비공개 객체 경로로 이미지 URL이 아니다.
- bookshelf 응답의 크기/본문 제외 방침은 유지한다. 스티커 상세와 동화 원문을 책장 목록에 추가하지 않는다.

스티커 지급은 DB의 채점 트랜잭션 안에서 수행한다. mastery 한 종류만 사용하며 기존 UNIQUE(profile_id,story_id,reward_kind)로 재도전·중복 제출에도 동화당 한 장을 보장한다. 과거 결과 자동 소급 지급은 하지 않으며 다시 풀어 획득할 수 있다. 신규 필수 환경변수 없음. 상세는 docs/STICKER_BOOK_IMPLEMENTATION.md 참조.


## 2026-09-18 공통 장면 설계와 답 전용 선택

생성 응답/저장 content.generation의 선택적 필드:

- planning_version: string|null (현재 scene-keyframes-theme-v2). 이전 동화는 생략 가능.
- planning_status: ready|fallback|null. fallback은 계획 실패 후 기존 집필 진행.
- prompt_version: 설계 사용 시 planned-story-v1, 설계 실패 시 기존 기본 프롬프트 버전.
- selection.review_version: story-choice-v5-answer-only.
- selection.coherence_score / level_fit_score / repair: null. 점수·수정안을 출력하지 않으므로 0점/통과 점수로 해석하지 않는다.
- selection.status: selected(선택 완료), single_candidate(유효 후보 하나), review_failed(판별 실패 후 유효 후보 사용). selected는 품질 보증이 아니다. 과거 selected_with_concerns 기록은 계속 읽을 수 있다.

내부 LLM 응답은 {"selected_candidate":"A"} 또는 B뿐이며 상세 검토 원문·계획 본문은 공개하지 않는다. 작업 진행은 기존 write_candidate 단계를 설계부터 집필 완료까지 사용한다. 신규 요청 필드/작업 상태/인증/필수 환경변수/DB 변경 없음. 상세: backend docs/COMPACT_STORY_CHOICE.md.


## 2026-09-18 최근 생성 원전 반복 완화 (점수 가중치)

동기 POST /api/v1/stories/generate 및 비동기 generation-jobs의 요청/응답 필드는 유지한다.
서버는 인증 계정 전체에서 source_id가 있는 최근 저장 완료 동화 두 편을 조회해, 가장 최근 원전 점수에 0.2, 직전 원전 점수에 0.6을 곱한다. 두 편의 원전이 같으면 강한 감점 한 번만 적용한다. 원전을 후보에서 제외하지 않는다. 비동기 작업은 실행 직전에 이력을 조회한다.

Qdrant는 기존 후보 5개 대신 감점 대상 수만큼 최대 7개를 받아 조정 후 5개를 반환한다. 로컬 검색 폴백도 같은 감점을 사용한다. 선택적 LLM 재정렬은 점수를 반환하지 않으므로 1/순위에 가중치를 적용한다. 음수 벡터 점수는 가중치로 나누어 낮춘다. 이력 조회 장애 시 일반 검색으로 진행한다.

generation.retrieval_mode의 기존 검색 방식 문자열에 실제 감점 적용 시 +recency_weighted가 붙는다. 예: qdrant+recency_weighted 또는 qdrant+rerank+recency_weighted. 서버 전용 _recent_source_ids/점수는 공개 요청·응답에 추가하지 않는다.

선택 환경변수 RECENT_SOURCE_LATEST_WEIGHT / RECENT_SOURCE_PREVIOUS_WEIGHT로 조정 가능하다(0 초과 1 이하, 기본 0.2/0.6, 1은 감점 해제). 환경변수 추가 없이도 기본값으로 동작한다. DB 마이그레이션·Qdrant 재적재 불필요. 삭제된 동화/원전 기록 없는 옛 동화는 이력에서 제외한다. 상세: backend docs/RECENT_SOURCE_RANKING.md.

### LLM 원전 재정렬 기본 활성화

이번 배포부터 RERANK_MODEL 미설정 시 gemini-3.8-flash low로 후보를 재정렬한다. 명시적인 빈 값/공백 설정은 비활성화한다. 읽기 타임아웃10초, 오류 시 기존 검색 점수+최근 사용 가중치로 계속한다. 성공시 retrieval_mode에 +rerank, 호출 오류시 +rerank_failed가 붙는다. 환경변수 신규 등록/DB 변경은 필요하지 않다.


## 2026-09-19 안전 차단 오류

- 동기 `POST /api/v1/stories/generate`: 안전 차단 시 HTTP **422**, `X-Generation-Error: safety_blocked`, 응답 `{"detail":"아이와 함께 읽기 어려운 내용이 감지되었어요. 이름이나 소재, 오늘의 일을 바꿔 주세요."}`. 기존 detail 문자열 구조 유지.
- 비동기 생성 작업: 기존 응답 필드에서 `status: failed`, `stage: failed`, `error_code: safety_blocked`, `result: null`. 프론트는 일반 재시도 안내 대신 입력 변경 안내를 표시한다. 안전 차단 후 자동 재생성하지 않는다.
- 스티커 생성 안전 차단: 자산 상태가 `failed`가 되고 자동 재시도하지 않는다. 스티커 목록의 기존 `asset_status` 필드로 표시하며 지급/목록 구조는 동일하다.
- 공급자의 명시적 안전 거절만 처리한다. 사전 입력 검사나 모든 부적절한 내용의 차단을 보장하는 필터는 아니다. 이미 시작한 병렬 호출의 비용은 취소되지 않는다. 환경변수 추가 없음.

## 2026-09-19: 두 가지 동화 모드와 연령별 주제

### 주제 목록

`GET /api/v1/stories/themes?profile_id={id}&mode=original` (mode: original | personalized, 기본 original)

로그인 JWT 필수. 서버가 소유 프로필의 birth_date로 만 나이를 계산한다. 클라이언트는 나이·레벨·주제 설명을 보내지 않는다.

```json
{"version":"age-topics-v1","mode":"original","topics":[
  {"theme_id":"T13","label":"서로를 생각하는 가족","category":"가족","description":"옛이야기 속에서 만나는 서로를 생각하는 가족"}
]}
```

위는 항목 예시다. 전체 허용 목록에서 프론트가 중복 없이 최대3개를 셔플한다. reroll은 직전 카드에 없던 항목 우선, 부족한 수는 기존 풀에서 보충한다. 전체 허용 주제가3개 이하이면 reroll 비활성화. 기본 카드 문구는 “오늘은 이런 동화를 준비했어요”다.

- 0~2세 S, 3~5세 P, 6~8세 T, 9세 이상 F(팀원 기획안은9~10세, 그 이상은 상위군 재사용).
- 읽기1~20단계와 나이별 주제군을 연결해 추정하지 않는다.
- 내 이야기에는52개 중 해당 연령군, 옛이야기에는 원전 연결을 설정한15개 중 해당 연령군을 반환한다. 연령 제한 원전은 후보에서 제외한다.
- 생년월일 오류:422, 미인증:401, 타인 프로필:기존 소유권 오류. 빈 후보는 topics=[]이며 제한을 우회하지 않는다.

### 생성 요청 (동기/비동기 공통)

`POST /stories/generate` 및 `POST /stories/generation-jobs`에 `mode`, `theme_id` 추가.

```json
{"profile_id":"...","assessment_type":"posttest","mode":"original","theme_id":"T13","page_images":false,"tts":false}
```

```json
{"profile_id":"...","assessment_type":"posttest","mode":"personalized","theme_id":"T10","protagonist_name":"도란이","favorite":"공룡","today_event":"함께 놀았어요","page_images":false,"tts":false}
```

| 필드 | 규칙 |
|---|---|
| mode | original=옛이야기, personalized=내 이야기. 미지정은 기존 클라이언트용 personalized; 새 UI 기본은 original |
| theme_id | 서버 목록의 S/P/T/F+두 자리 ID. original 필수. personalized는 구버전 호환용으로 생략 가능 |
| protagonist_name | personalized 필수1~30자. original은 생략 가능하며 보내도 사용하지 않음 |
| favorite / today_event | personalized: theme_id가 있으면 둘 다 비워도 됨. 주제가 없으면 기존처럼 하나 이상 필요 |

다른 연령대/알 수 없는/원전 모드에서 미지원 주제는 생성 전422. 접수·worker 실행 시 재검증. 기존 이미지 설정, Idempotency-Key,202/job polling, 오류 응답과 stage 계약은 유지한다.

### 결과의 generation 메타데이터

```json
{"mode":"original","theme_id":"T13","theme_label":"서로를 생각하는 가족","topic_version":"age-topics-v1","source_title":"의좋은 형제"}
```

기존 generation 필드에 추가된다. 기존 저장 책의 누락을 허용한다. original 경로는 planning_version=original-plan-v1, prompt_version=original-story-v1, review_version=original-choice-v1. 주제 개인화는 topic-plan-v1.1 / topic-personalized-v1.1 / topic-choice-v1.1. 주제 없는 기존 요청은 이전 버전 유지.

DB 스키마/환경변수 추가 없음. backend를 먼저 배포한 후 새 frontend를 배포한다.

### 2026-09-19 나만의 이야기 직접 입력 주제

- 동기/비동기 생성 요청에 `custom_topic?: string` 추가(앞뒤 공백 제거, 최대 200자, 기본 빈 문자열).
- `mode=personalized`에서만 허용하며 `theme_id`와 동시 지정하면 422. 추천 카드를 고르면 직접 입력을 비우고, 직접 입력하면 카드 선택을 해제한다.
- 이름 + 직접 입력 주제만으로도 접수 가능. 기존 이름 + 관심사/경험 요청도 호환한다. 프로필 소유권과 서버 나이 검증은 동일하게 적용한다.
- 직접 입력은 원전/어휘 검색, 설계, 집필, 후보 판단의 주제 데이터로 사용한다. 시스템 지침을 대체하지 않는다.
- 직접 입력 동화의 `generation.theme_id=null`, `theme_label=입력 주제`, `topic_version=age-topics-v1`. 새 테이블/환경변수 없음.
- UI 탭명: 옛이야기 / 나만의 이야기. 접수 성공 시에만 선택/직접 입력을 초기화하고 다음 주제 카드를 뽑는다. 실패하거나 접수 여부가 불명확하면 작성 내용을 유지한다.

## 2026-09-20 부모님 성장 기록

### GET `/api/v1/parent/profiles/{profile_id}/growth`

로그인 JWT 필수. 소유한 아이 프로필만 조회한다. 다른 계정/없는 프로필은 404, 미로그인 401, 잘못된 조회 범위는 422, DB 장애는 503.

| 쿼리 | 기본값 | 허용 범위 |
|---|---|---|
| `days` | 42 | 7, 42, 90, 365 |
| `offset` | 0 | 0–100000 정수, 최근 결과지 20개씩 |

응답 구조(예시 값은 설명용):

```json
{
  "profile_id": "profile-id", "level": 12.2, "days": 42,
  "since": "2026-08-09T15:00:00+00:00", "as_of": "2026-09-20T00:00:00+00:00",
  "tier": {"name": "나무", "min_level": 12, "max_level": 16},
  "summary": {"books_total": 4, "books_read": 3, "questions_total": 24, "questions_correct": 19},
  "growth": [{"id": "result-id", "created_at": "2026-09-19T00:00:00+00:00", "level": 12.2, "kind": "post-story", "baseline": false}],
  "skills": [{"name": "어휘", "total": 10, "correct": 8}],
  "recent_results": [{"id": "result-id", "kind": "post-story", "assessment_type": "posttest", "created_at": "2026-09-19T00:00:00+00:00", "correct_answers": 8, "total_questions": 10, "level_after": 12.2, "is_practice": false, "story_title": "저장된 실제 동화 제목", "cover_color": "#698976"}],
  "results_total": 8, "offset": 0,
  "week": {"assessments": 2, "books_read": 1}
}
```

- `level`, `tier`는 측정 전 null 가능. 배열은 데이터가 없으면 `[]`.
- `growth`: 한국 시간 하루 마지막 비재풀이 결과의 `level_after`. 조회 기간 이전 마지막 값이 있으면 시작일에 `baseline:true`로 포함한다. 기준점은 새 측정이 아니다. X축은 실제 시간, Y축만 관측 범위로 확대한다.
- `skills`: **현재 티어와 풀이 직전 level_before의 티어가 같은 모든 기록**에서 집계한다. 재풀이·체크리스트·boolean 정답이 없는 상세 항목 및 level_before 누락 결과 제외. 씨앗1–6/새싹7–11/나무12–16/숲17–20. 그래프의 `days`와 무관하다.
- `summary.questions_*`: 전체 기간 채점 가능한 상세 문항 수. 재풀이·체크리스트 제외. 최초 측정의 boolean 답도 포함한다. 분모 0일 때 프론트는 정답률을 0%로 만들지 않고 `—`로 표시한다.
- `books_total`, `books_read`: 현재 저장된 동화/서버 완독 기록 수.
- `recent_results`: 최신순(created_at,id DESC) 20개. 체크리스트·재풀이도 조회할 수 있다. 동화가 삭제되면 제목/색상이 null이며 `동화 퀴즈`로 표시한다. 동화 원문, 이미지, 문항 상세는 포함하지 않는다.
- `week`: 최근 7×24시간의 비재풀이 결과 수(체크리스트 포함), 서버 완독 수. 화면 요약 문장에 사용한다.
- 기존 GET `/parent/overview`, PUT `/parent/profiles/{id}/guidance` 형식은 유지한다.
- 신규 읽기 전용 RPC `parent_growth_report(text,integer,integer)` 적용 후 백엔드 → 프론트 순으로 배포. 환경변수 추가 없음.


### 2026-09-20 주제 카드 표시·다음 생성 초기화

- `/stories/themes` 응답 구조는 동일하다. `description`은 원전·개인화 모두 화면 전용 친근한 문구로 제공하며 생성용 교육 메시지를 그대로 노출하지 않는다.
- 직접 입력란 선택 시 theme_id 선택을 해제하고 카드 3개를 흐리게 표시한다. 카드 재선택 시 custom_topic을 비우고 선명도를 복원한다. 기존 상호 배타 API 규칙은 유지한다.
- 접수 성공 시 현재 폼은 마지막 입력으로 고정하고 다음 진입용 초기화 상태만 저장한다. 다음 생성 화면을 열 때 새 카드를 추첨한다. 접수 실패/응답 유실 시 입력 보존 정책은 동일하다.


### 2026-09-20 원전 스티커·부모님 열람 복귀

- 공개 API 요청/응답 필드의 변경은 없다. 스티커 준비 RPC 내부 source에 saved_stories.content.generation.mode를 저장한다. original 이외의 기존 데이터는 personalized로 호환한다.
- 원전 스티커: 본문 인물이 독자에게 인사한다. 개인화 스티커: 주인공 외의 인물이 주인공에게 인사한다. 만점일 때 동화당 한 장 지급 정책은 그대로다.
- 원전은 캐릭터 시트가 있으면 참조하고 없으면 인물 외형/화풍 텍스트로 생성한다. 근거 검증 실패의 공통 캐릭터는 원래 인물 그림을 참조하지 않는다.
- 부모님 화면의 책장은 /library?from=parent, 하위 본문/결과는 from=parent-library를 사용한다. 부모님 결과 직접 진입은 from=parent를 복습까지 유지한다. 스티커북 경유는 /stickers?from=parent 및 from=parent-stickers를 사용한다. 외부 URL을 복귀 주소로 받지 않는다.


## 2026-09-20 스티커북 페이지·휴지통·추억 스티커

### GET /api/v1/stickers
- 기존 JWT 및 profile_id 소유권 검사 유지.
- query: profile_id 필수, limit 기본 4(1~48), offset 기본 0, story_id 선택, trashed 기본 false. true면 휴지통만 반환한다.
- 응답: stickers, total(현재 탭/필터 총수), next_offset 유지. active_total, trash_total 추가(동일 story_id 필터 기준).
- sticker: trashed_at(ISO 날짜 또는 null), subject_kind(character | memory) 추가. 기존 kind 정보가 없는 스티커는 character로 표시한다. 내부 image_path는 응답에서 제거하고 image_url에 서명 URL을 제공한다.
- 정렬: 보관함 획득일 내림차순, 휴지통 이동일 내림차순. 동일 시간은 id로 정렬한다.

### POST /api/v1/stickers/manage
```json
{"profile_id":"profile-id","ids":["sticker-uuid"],"action":"trash"}
```
- action: trash(휴지통 이동), restore(복원), delete(영구 삭제).
- ids: 1~100개 UUID. 중복 ID는 한 번 처리. 응답: {"affected":1,"action":"trash"}.
- 인증 누락 401, 다른 계정/프로필 또는 없는 ID 포함 404, 잘못된 action/크기/UUID 422. 일반 보관함에서 바로 영구 삭제하면 409.
- 일괄 요청은 전부 검증 후 실행한다. 일부만 처리하지 않는다. UI는 휴지통 이동과 영구 삭제에 각각 확인 모달을 표시한다.
- 영구 삭제는 되돌릴 수 없다. 같은 동화에서 자동 재지급하지 않도록 최소 식별 기록을 남긴다. 이미지/대사 원문은 삭제 기록에 남기지 않는다.
- 참조가 없어진 생성 자산도 DB에서 제거하고 Storage 삭제 큐를 남긴다. 기존 worker가 최대 8개씩 정리, 장애 시 10분 뒤 재시도. 이미 그리는 중인 경로는 늦은 업로드를 고려해 지연 정리한다. 동화 표지/캐릭터 시트는 삭제하지 않는다. 다른 보상 기록이 공유하는 자산은 보존한다.

### 스티커 생성/표시
- 적절한 등장인물이 있으면 기존 character 인사. 원전은 독자에게, 개인화는 주인공에게 보낸다.
- 개인화에서 주인공밖에 없거나 인물 선택/근거 검증에 실패하면 memory(추억): 본문에 나온 물건이나 풍경 + 이야기를 기억하는 짧은 문구. 공통 책방 친구로 대체하지 않는다.
- 모델이 고른 memory는 물건/장소 이름과 정확한 인용을 검사한다. 출력 실패 시 실제 본문 장면을 이미지 모델에 전달해 추억으로 만든다. 추가 텍스트 모델 호출 없음. 안전 거절은 계속 중단하며 폴백으로 우회하지 않는다.
- memory에는 인물 시트를 참조하지 않는다. 프론트는 등장인물 대사 대신 '간직할 추억'으로 표시한다. 기존 완성 스티커는 자동 재생성하지 않는다.


## 2026-09-20 부모님 낱말 도감

`GET /api/v1/parent/profiles/{profile_id}/vocabulary`

- 로그인 JWT 필수. 소유 프로필만 조회(비로그인 401, 타인/없는 프로필 404).
- query: `pos`(빈값/명사/동사/형용사/부사), `q`(최대 80자, 기본형의 부분 문자열), `offset`(0~100000), `limit`(1~48, 기본 24).
- 잘못된 범위 422, 조회 실패 503. 실패를 빈 도감으로 반환하지 않는다.
- `summary`는 필터와 무관한 전체 요약, `total`은 필터 적용 후 개수다. 정렬은 최초 완독 날짜 내림차순 → 단어 → 품사다.

```json
{
  "profile_id": "예시프로필", "offset": 0, "limit": 24, "total": 1,
  "summary": {"total_words": 1, "new_this_week": 1, "by_pos": {"형용사": 1}, "completed_books": 2, "analyzed_books": 1, "pending_books": 1},
  "items": [{"profile_id": "예시프로필", "word": "포근하다", "pos": "형용사", "grade": 1,
    "story_count": 1, "occurrence_count": 2,
    "first_seen_at": "2026-09-20T00:00:00Z", "last_seen_at": "2026-09-20T00:00:00Z",
    "first_story_id": "예시동화", "story_title": "겨울 숲",
    "example": "이불은 참 포근했어요.", "surface": "포근했", "scene_index": 0, "text_offset": 6,
    "definitions": ["도톰한 물건이나 자리 따위가 보드랍고 따뜻하다."]}]
}
```

- 완독 기록이 있는 현재 보관 중인 동화 본문만 집계. 같은 기본형·품사는 한 종류, 다른 품사는 별도 종류.
- `story_count`는 등장 동화 수, `occurrence_count`는 본문 등장 횟수. 재독을 여러 번 세지 않는다.
- 처음/최근 날짜는 해당 동화들의 최초 완독 시각 기준. 분석이 늦게 도착해도 생성일/분석일로 대체하지 않는다.
- `new_this_week`는 한국 시간 월요일 00시 이후 최초 완독으로 만난 단어 수.
- `scene_index`는 0부터, `text_offset`은 장면 본문의 UTF-16 위치. 현재 UI는 장면 표시만 하고 특정 위치로 이동하지 않는다.
- 등급 CSV와 기본형·품사가 일치한 어휘만 공개 목록에 포함. 여러 사전 뜻이 있을 수 있으며 문맥에 맞는 뜻을 확정한 값이 아니다.
- 도감은 어휘 노출 기록이며 이해/습득률이 아니다. 제목까지 포함하는 기존 동화 수준 분석과 집계 범위가 다르다.
- 동화 삭제 시 그 동화 기여분 제거, 프로필 삭제 시 해당 기록 제거. 배치 재실행은 중복 가산하지 않는다.
- 내부 `content.generation.vocab_analysis.dictionary_version=1`로 백필 완료를 표시한다. 기존 `/vocab-analysis` 공개 응답 필드는 그대로다.


## 2026-09-20: 동화 신규 낱말 비교 / 생성 단계

- `GET /api/v1/stories/{story_id}/vocab-novelty?profile_id=...`: 로그인 JWT 필수. API·DB에서 소유 프로필/동화를 검증한다.
- 준비됨: `{status:"ready", basis:"before_story_created", as_of:"생성 시각", total_words:200, new_words:100, new_percent:50.0}`. 분모는 사전 등급이 있는 본문 기본형+품사의 고유 수이며 반복 횟수가 아니다. 생성 시각 이전 완독 동화에 없는 낱말만 분자에 포함. 아이의 실제 어휘 습득/미습득 판정이 아니다.
- 대상 분석 또는 과거 완독 책 분석이 미완료면 `{status:"pending", pending_books?:N}`. 생성 시각이 없으면 `{status:"unavailable"}`. 총 단어 0이면 ready/0/0/null로 응답. 401 미인증, 404 소유권/동화 없음. 분석 지연·오류는 독서를 막지 않는다.
- 기존 GitHub Actions가 `story_vocab_words`를 저장한 뒤, 분석 창을 열었을 때 한 번의 집합 비교 RPC로 조회한다. 표지/생성 경로에는 이 조회를 추가하지 않는다. 화면 pending 조회는 최대 6회, 15초 간격이며 창을 닫으면 중단한다.
- `GET /stories/bookshelf`의 `stories[]`, `resume`, `latest_unread`에 `generation_level: integer|null` 추가. `content.generation.level` 우선, 과거 데이터는 `content.quizzes[0].level` 사용. 마이그레이션에서 완료된 생성 작업의 저장 레벨이 있으면 기존 동화 메타데이터를 보완한다. 그래도 저장 정보가 없으면 null이며 현재 프로필 레벨로 추정하지 않는다.
- 신규 동화 `generation.level`은 생성 때 서버에서 적용한 1~20 정수 단계를 보존한다. 부모님 전체 책장에서 기존 씨앗/새싹/나무/숲 단계 라벨로 표시한다.


### 2026-09-21 원전 미연결 주제 추천 제외

- 주제 목록 응답 `version` 및 새 동화의 `generation.topic_version`: `age-topics-v2`.
- S01/S03/S04/P01/P02/P03/T01은 원전 적합성 검토 후 추천에서 비활성화했다. 원본 52개는 보존하며 API에 내부 편집 상태/사유는 노출하지 않는다.
- 두 모드의 카드 조회와 생성 접수·worker 실행의 주제 ID 검증에 동일 적용. 비활성 ID를 보낸 오래된 요청은 기존 422 주제 재선택 응답을 받는다.
- 나만의 이야기의 연령별 추천 풀: 0~2세 9개, 3~5세 11개, 6~8세 13개, 9세 이상 12개(합계45개).
- 옛이야기는 기존 원전 연결/연령 필터도 유지하므로 현재 3/4/4/4개다. 간접 연결 20개를 추가로 제외하거나 검색 범위를 확장한 변경은 아니다.
- 기존 저장 동화/결과와 직접 입력 주제는 영향 없음. 요청·응답 필드 구조, DB 및 환경변수 변경 없음.


## 2026-09-21 — 옛이야기 원전 연결 확대 (age-topics-v3)

- `GET /api/v1/stories/themes`의 `mode=original`도 수동 제목/태그 연결표로 주제를 제한하지 않는다. 두 모드 모두 나이별 활성 주제 9/11/13/12개를 제공한다(전체 45개). 기존 제외 ID S01·S03·S04·P01·P02·P03·T01은 계속 제외한다.
- `version`과 새 동화 `generation.topic_version`은 `age-topics-v3`. `topics[].label`, 새 동화 `generation.theme_label`은 `display_label`에서 가져오므로 종전 `original_label=null`인 주제도 빈 제목이 되지 않는다. 요청 필드는 그대로다.
- 옛이야기 검색은 연령 허용된 등록 원전 전체를 대상으로 Qdrant 상위 최대 12편을 검색한다. 장애 시 로컬 검색에도 연령 제한 및 아래 적합성 검사를 적용한다. 수동 연결표는 역사 자료로 보존하지만 검색 필터로 사용하지 않는다.
- 기존 순위만 반환하던 재정렬 호출을 줄거리 근거를 확인하는 호출로 대체한다. `RERANK_MODEL`(기본 Gemini 3.8 Flash), low, HTTP timeout 20초, 출력 한도 4096토큰. 적합 후보 최대 5편과 각 줄거리의 연속 인용(8~180자)을 반환한다. 코드에서 ID·중복·인용 실재 여부를 검사한다. 의미적 적합성 자체는 모델 판단이며 완전한 보증은 아니다.
- 통과 후보에만 기존 최근 원전 가중치 0.2/0.6을 적용한다. 나만의 이야기는 기존 검색/재정렬/집필 방식을 유지한다. 옛이야기 집필은 원전 보존 지침을 유지하며 주제를 맞추기 위해 새 사건을 강제하지 않는다.

### 새 실패 코드

| 조건 | 비동기 작업 `error_code` | 동기 API | 안내 |
|---|---|---|---|
| 검사한 후보 중 적합한 원전이 없음 | `source_topic_unmatched` | 422 + `X-Generation-Error` | 다른 주제를 선택 |
| 검사 API 장애·출력 오류·근거 오류·RERANK_MODEL 공백 | `source_check_unavailable` | 503 + `X-Generation-Error` | 잠시 뒤 다시 시도 |

- 비동기는 접수 202 이후 `failed`/위 오류 코드를 반환한다. 프론트는 두 경우를 구분해 알린다. 자동 재생성하지 않는다.
- 두 경우 모두 어휘 검색·본문/이미지 생성·저장·어휘분석 트리거로 진행하지 않는다. 이미 사용한 원전 검색/검사 API 비용은 발생할 수 있다.
- `source_topic_unmatched`는 이번 상위 후보 내 부적합 판정이다. 전체 원전 140편에 연결 가능한 책이 절대 없다는 뜻은 아니다.
- DB 스키마 변경, Qdrant 재적재, 새 환경변수는 필요 없다. `RERANK_MODEL`을 공백으로 설정해 둔 환경에서는 옛이야기 생성을 위해 모델값을 복원해야 한다.
- 프론트 오류 안내 먼저 배포한 뒤 백엔드 배포를 권장. 이 문서는 로컬 구현 기준이며 아직 push/운영 반영하지 않았다.


## 2026-09-21: 선택 음성 생성 및 페이지별 재생 시간표

- 생성 모달의 음성 체크박스 → 기존 generation-jobs 요청의 `tts: true`. 기본은 꺼짐. 계정 DB 설정이 아닌 프로필별 작성 중 입력으로 보존한다.
- 본문 후보 선택 후 퀴즈·이미지·음성 분기가 합류한다. 음성은 장 하나씩 업로드하여 RAM에 누적하지 않는다.
- `pages[]`에 `audio_path: string|null`, `audio_duration: number|null`(초), `audio_sha256: string|null` 추가. 음성은 비공개 `story-audio` 버킷. 만료되는 URL은 DB에 저장하지 않는다.
- 기존 `not_implemented` 책은 음성이 없으므로 재생기를 표시하지 않는다. 모든 장이 ready인 경우만 전체 동화 재생기를 표시한다. 부분 실패는 글/그림을 유지하고 음성 준비 실패 안내를 표시한다.

### POST `/api/v1/stories/{story_id}/narration-layout`

JWT 필수. 해당 계정의 프로필과 동화 소유권을 먼저 검사한다.

```json
{"profile_id":"아이 프로필 ID","chapter":1,"ranges":[{"start":0,"end":80},{"start":80,"end":160}]}
```

- ranges는 그 장 전체 본문의 연속 UTF-16 위치. 프론트 `startOffset/endOffset`을 그대로 보낸다. 누락·겹침·임의 문장·임의 음성 URL은 허용하지 않는다.
- 서버가 저장 본문과 음성을 직접 조회하여 각 페이지의 종료 시각만 Gemini로 얻는다. 마지막 종료는 실제 저장 음성 파일 길이(MP3, 기존 WAV 호환). 첫 시작=0, 이후 시작=직전 종료.
- 한 쪽짜리 장은 실제 길이만 사용하고 추가 모델 호출하지 않는다.

```json
{"audio_url":"https://.../storage/v1/object/sign/...","duration":53.6,"ends":[17.59,32.22,44.97,53.6],"layout_id":"sha256...","expires_in":7200}
```

- 동일 음성·페이지 본문 구성은 영속 캐시를 재사용하고 URL만 재발급한다. 서명 URL 만료는 2시간.
- 초기 재생 시 각 장을 순서대로 준비한다. 오류를 자동 유료 재시도하지 않는다. 한 장당 시간표 구성 최대 8종. 실패 슬롯도 포함하며 중단 슬롯은 관리자가 확인해야 한다.
- 401 로그인 없음, 404 타인/없는 동화, 422 잘못된 페이지 구간, 409 음성 미준비/다른 분석 진행/중단된 시간표, 429 구성 한도, 503 저장·분석 실패.
- 웹 프로세스 1개 전제의 정렬 잠금. 멀티프로세스/다중 인스턴스로 확장하기 전 DB 잠금으로 옮겨야 한다.

### 재생 UX

- 음성책 표지 안내: “읽어 주는 동화예요. 화면 아래쪽의 재생 버튼을 눌러 보세요.”
- 하단 오버레이: 재생/일시정지, 전체 음성 시간 seek, 장·쪽별 이동 버튼. 시간은 음성 합계이며 쉬는 시간은 포함하지 않는다.
- 같은 장은 음성을 연속 재생하며 저장된 시간표에 맞춰 책장만 전환한다. 쪽 경계에서는 정지·탐색하지 않고, 실제 음성 종료 후 장 사이에만 1.2초 쉰다. 모바일 삽화 전용 쪽은 장 시작 때 잠시 보여 준다.
- 재생 중 책 클릭/스와이프/번호 이동/글자 크기 조작 잠금. 오버레이의 탐색은 가능하다. 일시정지 후 수동 이동한 쪽부터 재생.
- 화면 방향/글자 크기로 페이지 구성이 바뀌면 멈추고 해당 구성 시간표를 준비한다. 창 숨김/화면 이탈 시 일시정지한다. 동화 생성 작업 자체의 백그라운드 실행에는 영향 없다.
- 마지막 음성까지 끝나면 완독 화면으로 이동한다.

### 배포 순서

1. backend `python -B scripts/setup_story_audio.py --apply`로 비공개 버킷 준비(이번 개발 중 적용·확인).
2. 백엔드 배포 후 프론트 배포. 기존 GEMINI_API_KEY 또는 API_KEY 사용.
3. 선택 환경변수 기본값: TTS_MODEL=gemini-3.1-flash-tts-preview, TTS_VOICE=Sulafat, TTS_ALIGNMENT_MODEL=gemini-3.8-flash. 기본값 사용 시 Render에 새 키를 넣을 필요 없음.
4. 음성 선택 시 추가 모델 비용과 대기 시간이 생긴다. 이번 UI 검증은 기존 WAV와 모의 시간표를 썼으며 운영 전체 음성 생성 실측은 아직 하지 않았다.


### 2026-09-21 옛이야기 추천·근거 원전 검색 일치

- `GET /api/v1/stories/themes`의 version 및 새 동화 `generation.topic_version`은 `age-topics-v4-original-support`.
- 위 v3의 “두 모드 동일 45개” 설명을 대체한다. `original`은 현재 원문에 직접 연결 근거가 있는 21개(나이군별 4/3/7/7)를 제공한다. `personalized`는 기존 45개(9/11/13/12)와 직접 입력을 유지한다.
- 카드 자격과 생성 접수/worker 검증은 같은 기준을 사용한다. F09 등 옛이야기 지원 제외 ID의 오래된 선택은 기존 422 재선택 안내를 받는다. 프론트는 목록을 다시 불러올 때 유효하지 않은 선택을 해제한다.
- 현재 줄거리·주제 지문과 근거 인용, 로컬 연령/사용 정책을 확인한다. 정책 변경 시 카드 수가 줄 수 있다. 카드 API에서 원전 ID나 내부 근거를 노출하지 않는다.
- 옛이야기는 주제명+메시지로 검색하며, 검토 근거 원전이 상위 검색에 없으면 같은 필터로 Qdrant를 추가 조회한다. 원격 제한/미적재를 우회하지 않고 최대 12편을 기존 적합성 모델로 한 번 검사한다. 결과 없음/검사 장애 오류는 기존과 동일하다.
- 전체 카탈로그 195편 중 옛이야기 사용 정책 허용 171편을 검색한다. 신규 자료를 근거표 밖이라는 이유로 검색에서 제외하지 않는다. DB/환경변수/요청·응답 필드 변경 없음.

- 운영 배포 확인용 `GET /` 응답에 `topic_version` 문자열 추가. 기존 status/message 유지. 인증/개인정보 없이 추천 정책 배포 버전만 노출한다.


### 2026-09-21 생성 시 음성 구간 인덱싱

- 새 TTS 동화는 장별 음성 생성/업로드 후, 짧은 문장·어절 묶음(목표 48자, 단어 중간 분리 없음)의 실제 종료 시각을 한 번 분석한다. 생성 완료 전에 `content.pages[]`에 아래 정보를 저장한다.
  - `audio_index_status`: `ready` / `failed` / null(기존 동화).
  - `audio_cues`: `[{end_offset: UTF-16 본문 끝 위치, end_seconds: 장 음성의 종료 초}, ...]` 또는 null. 최대 200구간. 마지막 위치는 원문 길이, 마지막 시각은 audio_duration과 일치한다.
- `GET`으로 받은 동화 본문에 해당 필드가 포함된다. `POST /stories/{story_id}/narration-layout` 요청 형식은 유지한다. 새 인덱스가 있으면 요청한 페이지 끝 위치를 저장 시각과 연결하고 비공개 음성 URL을 발급한다. 응답의 `timing_source=generation_index`는 이 경로에서만 추가된다.
- 새 인덱스 경로는 Gemini 호출·오디오 재다운로드·페이지별 유료 분석·기존 ALIGN_SLOT 잠금 없이 동작한다. 프론트는 저장 경계 안에서 페이지를 나누고 책을 열 때 장별 링크를 병렬 준비한다. 페이지 크기/글자 크기 변경 시에도 모델 재분석을 하지 않는다.
- 인덱싱 실패 시 해당 장 audio_status=failed, audio_index_status=failed로 기록하고 텍스트 동화는 보존한다. 자동 유료 재시도 없음. 손상된 인덱스/맞지 않는 페이지 경계도 임의 시간 추정이나 유료 폴백 없이 오류를 반환한다.
- 기존 동화(null/누락)는 최초 재생 시 분석·캐시하는 기존 경로를 유지한다. 기존 동화를 자동 일괄 분석하지 않는다. 신규 사전 준비와 달리 구형 경로는 사용자 재생 클릭 전 호출하지 않는다.
- 책 본문 하단 및 음성 체크포인트는 장·쪽 복합 표기 대신 본문 페이지의 연속 숫자를 표시한다. 삽화/표지는 본문 쪽 번호에 포함하지 않는다.
- DB 마이그레이션·새 환경변수 없음. 생성 단계에 분석 시간이 추가되며 최초 재생의 모델 대기를 옮기는 변경이다. 실제 발화 경계 정확도는 기존 Gemini 음성 정렬 모델에 의존한다.

## 2026-09-21 장별 MP3 저장 및 병렬 음성 생성

- 신규 음성은 Gemini PCM 수신 직후 LAME로 24kHz mono 64kbps MP3로 변환한다. 저장 경로는 비공개 `story-audio/{story_id}/{page_number}.mp3`, Storage MIME은 `audio/mpeg`다. 파일을 페이지별로 자르지 않는다.
- 시간표 분석에도 최종 MP3를 보내며 `audio_duration`, `audio_sha256`은 이 MP3 기준이다. 인코딩 지연/패딩 때문에 PCM 길이를 그대로 재사용하지 않는다.
- 생성→압축→업로드→시간표 분석을 장별 작업으로 실행한다. 한 동화의 5장을 모두 병렬 처리한다. 프로세스 전체 공용 실행기와 TTS 수신/압축 슬롯은 최대 5개이며, 사용자마다 5개를 추가하지 않는다. 이미지용 2개 슬롯과는 분리하고 TTS 응답 크기 제한은 유지한다. MP3 시간표 분석도 공용 실행기 내에서 병렬 처리한다.
- 기존 `.wav` 경로와 저장된 시간표는 그대로 지원한다. 클라이언트 요청/응답 필드는 변하지 않으며 서명 URL로 동일하게 재생한다.
- 새 의존성: `lameenc==1.8.4`, `mutagen==1.47.0`. 별도 ffmpeg/로컬 모델/새 API 키 없음. 기존 비공개 버킷에 `audio/mpeg`만 추가 허용한다(`scripts/setup_story_audio.py --apply`).
- 같은 장의 음성은 연속 재생하며 시간표에 맞춰 책장만 전환한다. 페이지 경계의 pause/seek는 하지 않는다. 장 전환은 실제 음성 ended 후 처리한다.
- 운영 반영 확인: `GET /`의 `narration_version: "mp3-five-v1"`은 MP3 5장 병렬 코드 배포를 식별한다. 인증·음성 응답 계약은 기존과 동일하다.

## 2026-09-21 생성 접수 장애 복구

- 요청·응답 스키마는 유지한다. 접수 POST의 프론트 대기 상한은60초(기존30초)이며 POST를 자동 재전송하지 않는다.
- 백엔드는 DB GET/HEAD의 httpx 통신 장애만250ms 후 한 번 재시도한다. 쓰기·접수·선점·완료RPC는 자동 재전송하지 않는다. 지속 장애는503을 반환하며 조회 실패를 빈 결과로 바꾸지 않는다.
- 요청한 job_id가404이고 current가 다른 과거 완료/실패 작업이면 새 요청 성공으로 간주하지 않는다. 다른 탭의 queued/running 작업은 복구할 수 있다.
- 프론트 내부 submission_missing은 서버 error_code 추가가 아니다. 접수 실패 안내를 유지하면서 같은ID로 지연 접수를 조회하며 확인 버튼은 해당 추적을 정리한다. 서버에 늦게 접수된 작업을 취소하거나 재생성하지 않는다.
- 로그는 DB작업 이름, HTTP상태/예외 종류, 재시도 횟수/경과시간만 남긴다. 기존 일반 DB오류 문구만으로 RLS 문제나 ReadTimeout을 단정할 수 없다.

### 2026-09-21: 생성 실패 화면에서 재시도
- 신규 API나 응답 필드는 추가하지 않는다. 기존 GET /stories/generation-jobs/{job_id}, POST /stories/generation-jobs와 Idempotency-Key를 사용한다.
- 사용자가 재시도를 누르면 기존 작업을 조회한다. queued/running/completed는 복구하며 POST하지 않는다. 404(접수 불명)는 같은 키로 요청하고, failed가 확인된 경우 새 키로 요청한다. 사전 조회가 통신 오류/401이면 요청하지 않고 안내한다.
- safety_blocked, source_topic_unmatched, profile_unavailable은 같은 입력 자동 재시도 대상에서 제외한다.
- 입력은 해당 계정 컨트롤러 메모리에만 보관한다. 새로고침/로그아웃 뒤에는 실패 안내의 버튼으로 작성 화면을 열어 입력을 확인하도록 한다.
- 실패 상태는 대시보드 종이극장에서 표시한다. 확인 요청 중 버튼을 비활성화해 중복 클릭을 막는다.

### 2026-09-22: 나만의 이야기 읽기 목적 (배포 전)
- POST /stories/generate 및 POST /stories/generation-jobs 요청에 `reading_mode` 추가: `level_aligned`(실력 쑥쑥, 기본) / `relaxed`(편하게 읽기).
- `mode=personalized`에서만 relaxed 허용. original+relaxed는 422. 생략한 구버전 요청은 기존 방식 유지.
- `content.generation` 및 생성 응답에 `reading_mode`, `reading_guidance_version`(relaxed-reading-v1 또는 null), `level`(생성 당시 프로필 단계) 보존. level을 편하게 읽기의 검증된 텍스트 난도로 해석하지 않는다.
- 공개 작업 stage는 기존 값 유지. 편하게 읽기 전용 계획/집필/선택 노드도 같은 로딩 UI로 표시한다.
- 채점 및 결과 재조회 응답에 `reading_mode`, `progression_applied` 추가. 서버가 저장된 동화의 모드로 판정하며 답안 요청으로 진척도 제외 여부를 지정하지 않는다.
- 편하게 읽기의 점수/답안/독서 기록/만점 스티커는 유지. profiles.level과 진척 게이지는 변화 없음. 재제출/다시 풀기로 스티커가 중복 지급되지 않는다.
- 1~9단계의 기존 퀴즈 미제공 규칙은 그대로다. 10~20단계는 편안한 본문에 맞춘 7문제 제공.
- 하루 횟수 제한 및 선행 학습 조건 없음. 선택은 기존 작성 초안 복원 흐름으로 유지되며 계정 설정 저장 API는 추가하지 않는다.
- 배포 순서: backend_test/sql/20260922_relaxed_reading.sql 적용 → 백엔드 배포 → 프론트 배포. DB 적용 없이 프론트부터 공개하면 단계 제외가 보장되지 않는다. 이 작업에서는 운영 DB에 적용하지 않음.


### 2026-09-22 생성 체크포인트 복구

- 공개 요청/응답 필드, 폴링 간격, 기존 mode/reading_mode 및 stage 값은 동일하다. 프론트 POST 자동 재전송 정책도 유지한다.
- 내부 실행 권한(리스)은20초마다 갱신하고120초 동안 갱신이 없으면 같은 버전의 worker가 작업을 인계받는다. 실행은 최초1회+복구2회까지, 최초 시작 기준20분 이내다. 서비스가 내려가 있거나 대기 작업이 있으면 실제 재개는 더 늦을 수 있다.
- 저장된 중간 결과는 서버 전용이며 GenerationJobResponse에 노출하지 않는다. 재개 시 실제 저장된 결과는 재사용한다. 중간 저장은 비동기이므로 완료했어도 DB에 저장되지 못한 단계는 다시 수행할 수 있다.
- 외부 모델 응답 수신/Storage 업로드와 DB 저장 사이에 종료되면 해당 미확정 단계는 다시 호출될 수 있다. 전체 유료 호출의 exactly-once를 보장하지 않는다.
- 음성 audio_path는 경로를 직접 조합하지 않는 불투명 값이다. 신규 비동기 음성은 story_id/장번호-내용해시.mp3 형식을 쓸 수 있으며 기존 서명/시간표 API를 그대로 이용한다.
- DB migration 20260922_generation_checkpoints.sql 적용 후 백엔드를 배포해야 활성화된다. 이전 작업의 중간 결과를 소급 생성하지 않는다. 동기 generate 경로와 독립 스티커 작업 큐는 이번 재개 범위에 포함하지 않는다.

### 2026-09-22 중간 결과 저장 비동기화

- 공개 JSON/폴링/stage는 변경하지 않는다. 복구용 중간 결과는 생성 경로에서 저장 응답을 기다리지 않는다.
- 저장 실패나 대기열 한도 도달 시 동화 생성은 계속한다. 재시작하면 실제 DB에 남은 마지막 지점부터 복구하며 미저장 단계는 다시 호출될 수 있다.
- 최종 completed는 기존 원자적 동화 저장이 성공한 후 확정한다. 실행 권한·안전 차단 검사도 유지한다.
- 운영 SQL 적용 및 배포 전 상태다.

- 2026-09-22 운영 DB 마이그레이션 적용 완료. GET / 응답에 checkpoint_version: "async-nodes-v1" 진단 필드 추가. 사용자 정보/키는 포함하지 않으며 실제 배포된 코드 구분용이다.
